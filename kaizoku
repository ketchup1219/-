<!DOCTYPE html>
<html lang="ja">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0, user-scalable=no">
    <title>ルージュ・ガレオン - 伝説の始まり</title>
    <!-- Tailwind CSS -->
    <script src="https://cdn.tailwindcss.com"></script>
    <!-- Firebase SDK -->
    <script src="https://www.gstatic.com/firebasejs/10.8.1/firebase-app-compat.js"></script>
    <script src="https://www.gstatic.com/firebasejs/10.8.1/firebase-database-compat.js"></script>

    <style>
        @import url('https://fonts.googleapis.com/css2?family=M+PLUS+Rounded+1c:wght@700;900&display=swap');
        body { background-color: #0f172a; color: #f8fafc; overflow: hidden; touch-action: none; user-select: none; font-family: 'M PLUS Rounded 1c', sans-serif; }
        .bubbles-container { position: absolute; inset: 0; pointer-events: none; overflow: hidden; z-index: 0; }
        .bubble { position: absolute; bottom: -20px; background: rgba(255,255,255,0.15); border-radius: 50%; animation: rise infinite ease-in; }
        @keyframes rise { 0% { transform: translateY(0) scale(1); opacity: 0; } 10% { opacity: 1; } 90% { opacity: 1; } 100% { transform: translateY(-100vh) scale(1.5); opacity: 0; } }
        
        #gameCanvas { 
            background-color: #1e3a8a;
            background-image: url('ステージ背景.png'), linear-gradient(to bottom, #0284c7, #1e40af, #1e3a8a);
            background-size: cover; background-position: center; background-repeat: no-repeat;
            display: block; margin: 0 auto; width: 100%; height: 100%; max-width: 500px; 
            box-shadow: inset 0 0 50px rgba(0,0,0,0.6); 
        }

        .game-btn { transition: all 0.1s; box-shadow: 0 6px 0 rgba(0,0,0,0.4); transform: translateY(0); }
        .game-btn:active { box-shadow: 0 0px 0 rgba(0,0,0,0.4); transform: translateY(6px); }
        .game-btn:disabled { transform: translateY(0); box-shadow: none; }
        
        .logo-container { perspective: 1500px; width: 100%; display: flex; justify-content: center; align-items: center; position: relative; min-height: 280px; z-index: 10; }
        .logo-enhanced { width: 160%; max-width: 500px; max-height: 280px; object-fit: contain; animation: superGiantFloat 4s ease-in-out infinite, metallicShineSuper 5s ease-in-out infinite; filter: drop-shadow(0 10px 15px rgba(0,0,0,0.8)); }
        
        @keyframes superGiantFloat { 0%, 100% { transform: scale(1.1) translateY(0px) rotate(-2deg); } 50% { transform: scale(1.15) translateY(-15px) rotate(2deg); } }
        @keyframes metallicShineSuper { 0%, 85%, 100% { filter: drop-shadow(0 10px 15px rgba(0,0,0,0.8)) brightness(1); } 90% { filter: drop-shadow(0 10px 15px rgba(0,0,0,0.8)) brightness(1.5) hue-rotate(-10deg); } 95% { filter: drop-shadow(0 10px 15px rgba(0,0,0,0.8)) brightness(1.5) hue-rotate(10deg); } }
        
        .giant-title { font-size: 14vw; font-weight: 900; line-height: 1.1; letter-spacing: 0.05em; text-align: center; background: linear-gradient(to bottom, #fffde7 0%, #ffd700 30%, #ff4500 70%, #8b0000 100%); -webkit-background-clip: text; -webkit-text-fill-color: transparent; text-shadow: 0 2px 0 #b45309, 0 4px 0 #92400e, 0 6px 0 #78350f, 0 8px 0 #451a03, 0 10px 0 #000000, 0 15px 30px rgba(0, 0, 0, 0.9), 0 0 40px rgba(255, 215, 0, 0.6); animation: textFloatSuperUltra 3s ease-in-out infinite alternate; }
        @keyframes textFloatSuperUltra { from { transform: translateY(0px) scale(1) rotate(-1.5deg); } to { transform: translateY(-10px) scale(1.05) rotate(1.5deg); } }
        
        .holy-aura { position: absolute; top: 50%; left: 50%; width: 150vw; height: 150vw; max-width: 800px; max-height: 800px; transform: translate(-50%, -50%); background: radial-gradient(circle, rgba(255,215,0,0.2) 0%, rgba(255,69,0,0.1) 40%, transparent 70%); animation: rotateAura 20s linear infinite; pointer-events: none; z-index: 1; mix-blend-screen: screen; }
        @keyframes rotateAura { from { transform: translate(-50%, -50%) rotate(0deg) scale(1); } 50% { transform: translate(-50%, -50%) rotate(180deg) scale(1.1); } to { transform: translate(-50%, -50%) rotate(360deg) scale(1); } }
        
        .screen { animation: fadeIn 0.3s ease-out; }
        @keyframes fadeIn { from { opacity: 0; transform: scale(0.95); } to { opacity: 1; transform: scale(1); } }
        .pop-in { animation: popIn 0.5s cubic-bezier(0.175, 0.885, 0.32, 1.275); }
        @keyframes popIn { 0% { transform: scale(0); opacity: 0; } 80% { transform: scale(1.2); } 100% { transform: scale(1); opacity: 1; } }
        .shake-box { animation: shake 0.4s infinite; }
        @keyframes shake { 0% { transform: translate(1px, 1px) rotate(0deg) scale(1.2); } 25% { transform: translate(-3px, 0px) rotate(10deg) scale(1.2); } 50% { transform: translate(3px, 2px) rotate(0deg) scale(1.2); } 75% { transform: translate(-1px, -1px) rotate(-10deg) scale(1.2); } 100% { transform: translate(1px, -2px) rotate(0deg) scale(1.2); } }
        
        #ur-anim-layer { background: repeating-conic-gradient(from 0deg, #ff0 0deg 15deg, #f80 15deg 30deg); animation: spin-bg 2s linear infinite, colorPulse 1s infinite alternate; opacity: 0; pointer-events: none; transition: opacity 0.2s; }
        #ur-anim-layer.active { opacity: 1; }
        @keyframes spin-bg { 100% { transform: rotate(360deg) scale(2); } }
        @keyframes colorPulse { 0% { filter: hue-rotate(0deg) brightness(1); } 100% { filter: hue-rotate(360deg) brightness(1.5); } }
        
        .ur-text-flash { animation: flashText 1.5s ease-out forwards; text-shadow: 0 0 20px #fff, 0 0 40px #ff0, 0 0 60px #f00; }
        @keyframes flashText { 0% { transform: scale(0); opacity: 0; } 50% { transform: scale(2); opacity: 1; } 100% { transform: scale(1); opacity: 1; } }
        
        .bounce-arrow { animation: arrowBounce 0.5s infinite alternate cubic-bezier(0.175, 0.885, 0.32, 1.275); }
        @keyframes arrowBounce { 0% { transform: translateY(0) scale(1.1); filter: drop-shadow(0 4px 6px rgba(0,0,0,0.6)); } 100% { transform: translateY(-16px) scale(0.95); filter: drop-shadow(0 15px 12px rgba(250,204,21,0.5)); } }
        
        .confetti { position: absolute; top: -10px; z-index: 9999; animation: fall linear forwards; }
        @keyframes fall { to { transform: translateY(110vh) rotate(720deg); } }
        
        .tutorial-highlight { box-shadow: 0 0 0 9999px rgba(0, 0, 0, 0.75), 0 0 20px #facc15, inset 0 0 20px #facc15 !important; z-index: 55; position: relative; animation: pulse-border 1s infinite; pointer-events: auto; }
        @keyframes pulse-border { 0% { border-color: #facc15; } 50% { border-color: #ef4444; } 100% { border-color: #facc15; } }
        
        #happy-share-toast { position: absolute; top: -100px; left: 50%; width: 90%; max-width: 380px; transform: translateX(-50%); opacity: 0; pointer-events: none; transition: top 0.5s cubic-bezier(0.175, 0.885, 0.32, 1.275), opacity 0.5s; z-index: 9999; }
        #happy-share-toast.show { top: 60px; opacity: 1; pointer-events: auto; }
        
        @keyframes slideInChar { 0% { left: -50%; opacity: 0; transform: translateY(-50%) scale(0.5) rotate(-20deg); } 100% { left: 50%; opacity: 1; transform: translate(-50%, -50%) scale(1) rotate(0deg); } }
        @keyframes slideInName { 0% { right: -50%; opacity: 0; transform: translateY(-50%) scale(1.5) skewX(-20deg); } 100% { right: 50%; opacity: 1; transform: translate(50%, -50%) scale(1) skewX(-10deg); } }
        @keyframes flashLightning { 0% { opacity: 1; background-color: #fff; } 10% { opacity: 0; } 20% { opacity: 0.8; background-color: #e0f2fe; } 30% { opacity: 0; } 40% { opacity: 1; background-color: #fff; } 100% { opacity: 0; } }
        .cutin-char-anim { animation: slideInChar 0.4s cubic-bezier(0.175, 0.885, 0.32, 1.275) forwards; }
        .cutin-name-anim { animation: slideInName 0.4s cubic-bezier(0.175, 0.885, 0.32, 1.275) 0.1s forwards; }
        .flash-anim { animation: flashLightning 0.6s ease-out; }
        
        .no-scrollbar::-webkit-scrollbar { display: none; }
        .no-scrollbar { -ms-overflow-style: none; scrollbar-width: none; }
    </style>
    <script>function fb(el, em, cls="text-sm drop-shadow-md") { el.outerHTML = `<span class='${cls}'>${em}</span>`; }</script>
</head>
<body class="flex justify-center items-center h-screen w-screen bg-slate-950 relative">

    <img id="img-seimaro-boss" src="せいまろ.png" class="hidden" alt="boss">
    <div class="bubbles-container" id="bubbles-layer"></div>

    <div id="app" class="relative w-full h-full max-w-md bg-indigo-950 shadow-2xl overflow-hidden flex flex-col border-x-4 border-indigo-900">
        
        <!-- ヘッダー -->
        <header id="app-header" class="bg-indigo-900 p-2 flex justify-between items-center border-b-4 border-indigo-800 z-20 shadow-md">
            <div class="flex items-center gap-2 cursor-pointer transition-transform transform hover:scale-105 active:scale-95" onclick="openProfileModal()">
                <div id="ui-usericon" class="w-10 h-10 bg-gradient-to-br from-yellow-400 to-orange-500 rounded-full flex items-center justify-center font-bold text-xl shadow-[0_2px_0_#9a3412] border-2 border-white overflow-hidden">👤</div>
                <div class="flex flex-col hidden sm:flex">
                    <span class="text-[10px] text-yellow-200 font-bold leading-none tracking-widest">CAPTAIN</span>
                    <span id="ui-username" class="font-black text-sm text-white leading-tight drop-shadow-md w-16 truncate">プレイヤー</span>
                </div>
            </div>
            
            <div class="flex items-center gap-2 text-sm font-bold bg-indigo-950 px-2 py-1.5 rounded-full border-2 border-indigo-700 shadow-inner overflow-x-auto no-scrollbar max-w-[75%]">
                <div class="flex items-center gap-0.5 pop-in whitespace-nowrap cursor-pointer active:scale-95 bg-indigo-900/80 px-2 py-0.5 rounded-lg border border-yellow-500/50 shadow-sm" onclick="openStaminaModal()">
                    <span class="text-yellow-400 drop-shadow-md text-xs">⚡</span>
                    <span id="ui-stamina" class="text-white text-xs drop-shadow-md font-black tracking-wider">100/100</span>
                    <span class="bg-green-500 text-white rounded-full text-[8px] w-3 h-3 flex items-center justify-center ml-0.5 shadow-sm">+</span>
                </div>
                <div class="flex items-center gap-1 pop-in whitespace-nowrap pl-1">
                    <span class="inline-flex items-center justify-center relative w-4 h-4"><img src="草コイン.png" class="absolute inset-0 w-full h-full object-contain z-10" onerror="fb(this,'💰','text-xs')"></span>
                    <span id="ui-coins" class="text-yellow-300 text-xs drop-shadow-md font-black">0</span>
                </div>
                <div class="flex items-center gap-1 pop-in whitespace-nowrap">
                    <span class="inline-flex items-center justify-center relative w-4 h-4"><img src="草の化石.png" class="absolute inset-0 w-full h-full object-contain z-10" onerror="fb(this,'🦴','text-xs')"></span>
                    <span id="ui-materials" class="text-gray-300 text-xs drop-shadow-md font-black">0</span>
                </div>
                <div class="flex items-center gap-1 pop-in whitespace-nowrap border-l border-indigo-700 pl-1">
                    <span class="inline-flex items-center justify-center relative w-4 h-4"><img src="草核.png" class="absolute inset-0 w-full h-full object-contain z-10" onerror="fb(this,'✨','text-xs')"></span>
                    <span id="ui-tickets" class="text-white text-base drop-shadow-md">0</span>
                </div>
            </div>
        </header>

        <main class="flex-1 relative overflow-y-auto no-scrollbar pb-20">
            <!-- 1. ホーム画面 -->
            <div id="screen-home" class="screen p-4 flex flex-col items-center justify-between min-h-full gap-2 relative overflow-hidden">
                <div class="holy-aura"></div>
                <div class="logo-container mt-2">
                    <div class="relative w-full flex justify-center items-center">
                        <div id="title-fallback" class="hidden giant-title">ルージュ<br>ガレオン<br><span class="text-xl text-yellow-300 font-black tracking-widest block mt-4" style="text-shadow: 0 2px 4px #000;">〜伝説の大冒険〜</span></div>
                        <img id="title-logo" src="ルージュ・ガレオン.png" class="absolute logo-enhanced z-10" onerror="showLogoFallback(this)">
                    </div>
                </div>
                <div id="home-equip-preview-box" class="bg-indigo-900/80 p-4 rounded-2xl w-full text-center border-4 border-indigo-700 shadow-xl z-10 relative mt-auto mb-2 backdrop-blur-sm">
                    <div class="absolute -top-3 left-1/2 transform -translate-x-1/2 bg-yellow-500 text-indigo-950 font-black text-xs px-4 py-1 rounded-full shadow-md border-2 border-white">現在の艦隊</div>
                    <div class="flex justify-around items-end h-16 mt-2">
                        <div class="flex flex-col items-center w-1/3">
                            <span class="text-[10px] text-indigo-300 font-bold">海賊船</span>
                            <span id="home-skin-icon" class="text-4xl mb-1 filter drop-shadow-lg pop-in"></span>
                            <span id="home-skin" class="font-bold text-yellow-200 text-xs w-full truncate px-1"></span>
                        </div>
                        <div class="flex flex-col items-center w-1/3 border-l-2 border-r-2 border-indigo-700">
                            <span class="text-[10px] text-indigo-300 font-bold">大砲</span>
                            <span id="home-weapon-icon" class="text-4xl mb-1 text-slate-300 filter drop-shadow-lg pop-in">💣</span>
                            <span id="home-weapon" class="font-bold text-cyan-300 text-xs w-full truncate px-1"></span>
                        </div>
                        <div class="flex flex-col items-center w-1/3">
                            <span class="text-[10px] text-indigo-300 font-bold">乗組員</span>
                            <span id="home-crew-container" class="w-10 h-10 mb-1 flex items-center justify-center filter drop-shadow-lg pop-in"></span>
                            <span id="home-crew" class="font-bold text-pink-300 text-xs w-full truncate px-1"></span>
                        </div>
                    </div>
                </div>
                <button id="btn-start-navigation" onclick="navTo('screen-stage')" class="game-btn w-full py-4 bg-gradient-to-b from-yellow-300 to-orange-500 rounded-2xl font-black text-2xl text-indigo-950 border-2 border-white z-10 relative flex justify-center items-center gap-2">
                    <span>大冒険へ出発!!</span> <span>🌊</span>
                </button>
            </div>

            <!-- 2. ステージ選択画面 -->
            <div id="screen-stage" class="screen hidden p-4 flex flex-col gap-4">
                <div class="bg-indigo-900 rounded-xl p-3 border-2 border-indigo-700 shadow-md text-center flex-shrink-0">
                    <h2 class="text-xl font-black text-transparent bg-clip-text bg-gradient-to-r from-cyan-300 to-blue-300">🗺️ 航路図（ステージ）</h2>
                    <p class="text-xs text-indigo-300 mt-1 font-bold">ライバルを追い払って財宝をゲットしよう！</p>
                </div>
                <div class="w-full flex bg-indigo-950 rounded-xl p-1 border-2 border-indigo-800 shadow-inner flex-shrink-0">
                    <button id="tab-stage-normal" onclick="switchStageTab('normal')" class="flex-1 py-2 rounded-lg font-black text-xs bg-indigo-600 text-white shadow-md transition-all border-b-4 border-indigo-800">通常</button>
                    <button id="tab-stage-ex" onclick="switchStageTab('ex')" class="flex-1 py-2 rounded-lg font-black text-xs text-indigo-400 hover:text-white transition-all relative border-b-4 border-transparent">EX</button>
                    <button id="tab-stage-multi" onclick="switchStageTab('multi')" class="flex-1 py-2 rounded-lg font-black text-xs text-indigo-400 hover:text-white transition-all relative border-b-4 border-transparent">マルチ🤝</button>
                </div>
                <div id="stage-list" class="flex flex-col gap-4 pb-8 overflow-y-auto pr-2 no-scrollbar"></div>
            </div>

            <!-- 3. 編成・強化画面 -->
            <div id="screen-equip" class="screen hidden p-4 flex flex-col gap-4">
                <div class="bg-indigo-900 rounded-xl p-3 border-2 border-indigo-700 shadow-md text-center">
                    <h2 class="text-xl font-black text-transparent bg-clip-text bg-gradient-to-r from-green-300 to-emerald-300">⚓ 編成・強化</h2>
                    <p class="text-xs text-indigo-300 mt-1 font-bold">お宝をタップしてレベルアップ＆装備だ！</p>
                </div>
                <div class="bg-indigo-900/80 p-3 rounded-xl border-2 border-indigo-700 shadow-lg">
                    <h3 class="text-sm font-black text-blue-300 mb-2">⛵ 海賊船（スピードUP！）</h3>
                    <div id="equip-skins" class="grid grid-cols-2 gap-3"></div>
                </div>
                <div class="bg-indigo-900/80 p-3 rounded-xl border-2 border-indigo-700 shadow-lg">
                    <h3 class="text-sm font-black text-cyan-300 mb-2">💣 大砲（弾速・連射UP！）</h3>
                    <div id="equip-weapons" class="grid grid-cols-2 gap-3"></div>
                </div>
                <div class="bg-indigo-900/80 p-3 rounded-xl border-2 border-indigo-700 shadow-lg">
                    <h3 class="text-sm font-black text-pink-300 mb-2">🦜 乗組員（パワー・ポイントUP！）</h3>
                    <div id="equip-crew" class="grid grid-cols-2 gap-3"></div>
                </div>
                <div class="bg-indigo-900/80 p-3 rounded-xl border-2 border-indigo-700 shadow-lg mt-2">
                    <h3 class="text-sm font-black text-yellow-400 mb-2">🔋 スタミナ上限（最大値UP！）</h3>
                    <div id="equip-system" class="grid grid-cols-1 gap-3"></div>
                </div>
            </div>

            <!-- 4. ガチャ画面 -->
            <div id="screen-gacha" class="screen hidden p-4 flex flex-col items-center justify-start min-h-full gap-3 relative z-10">
                <div class="w-full flex bg-indigo-950 rounded-xl p-1 border-2 border-indigo-800 mt-1 shadow-inner">
                    <button id="tab-gacha-normal" onclick="switchGachaTab('normal')" class="flex-1 py-2 rounded-lg font-black text-sm bg-indigo-600 text-white shadow-md transition-all border-b-4 border-indigo-800">🌊 わくわく通常</button>
                    <button id="tab-gacha-ex" onclick="switchGachaTab('ex')" class="flex-1 py-2 rounded-lg font-black text-sm text-indigo-400 hover:text-white transition-all relative border-b-4 border-transparent">
                        ⭐ 超・限定EX<span class="absolute -top-2 -right-2 bg-pink-500 text-white text-[10px] px-2 py-0.5 rounded-full shadow-md animate-bounce border border-white">超オトク!</span>
                    </button>
                </div>
                <div class="w-full flex justify-between items-center px-1">
                    <div class="text-left">
                        <h2 id="gacha-title" class="text-2xl font-black text-transparent bg-clip-text bg-gradient-to-b from-yellow-200 to-yellow-500 drop-shadow-md">伝説の宝箱ガチャ</h2>
                        <p id="gacha-sub" class="text-[10px] text-yellow-100 font-bold bg-indigo-900 px-2 py-0.5 rounded-full inline-block border border-indigo-700">ドキドキ！何が出るかな！？</p>
                    </div>
                    <button onclick="openGachaProbabilityModal()" class="game-btn px-3 py-2 bg-indigo-900 border border-indigo-700 hover:bg-indigo-800 text-yellow-300 text-xs font-black rounded-xl shadow-md">📋 確率・一覧</button>
                </div>
                
                <div class="relative w-full max-w-[200px] aspect-square flex items-center justify-center">
                    <div class="absolute inset-0 bg-yellow-400/30 rounded-full blur-2xl animate-pulse"></div>
                    <div id="gacha-chest-emoji" class="text-[7rem] relative z-10 drop-shadow-[0_10px_20px_rgba(0,0,0,0.8)] transition-transform hover:scale-110">🏴‍☠️</div>
                    <div id="ex-pickup-badge" class="absolute bottom-0 bg-gradient-to-r from-pink-500 to-purple-600 border-2 border-white rounded-xl p-2 flex items-center gap-2 z-20 shadow-xl hidden">
                        <div class="w-10 h-10 rounded-lg bg-indigo-950 border-2 border-yellow-300 overflow-hidden flex items-center justify-center relative shadow-inner">
                            <span class="absolute text-lg">🐾</span>
                            <img src="せいまろ.png" class="absolute inset-0 w-full h-full object-contain z-10" onerror="this.style.opacity='0';">
                        </div>
                        <div class="text-left"><div class="text-[10px] text-yellow-300 font-black leading-none">超・大当り中!!</div><div class="text-sm text-white font-black">雷戦艦＆せいまろ</div></div>
                    </div>
                </div>
                
                <div class="w-full bg-indigo-900/90 p-3 rounded-2xl border-2 border-indigo-700 shadow-xl backdrop-blur mt-2">
                    <div class="flex justify-between text-xs mb-1.5">
                        <span class="text-indigo-200 font-black">🌟 超激レア(UR) 確率アップ中！</span><span id="gacha-rate" class="text-yellow-400 font-black text-sm">1.0%</span>
                    </div>
                    <div class="w-full bg-indigo-950 h-3 rounded-full overflow-hidden border-2 border-indigo-800 shadow-inner">
                        <div id="pity-bar" class="bg-gradient-to-r from-yellow-400 via-orange-500 to-pink-500 h-full w-0 transition-all duration-500"></div>
                    </div>
                </div>

                <div class="w-full flex gap-3 px-1 mt-1 z-20 relative">
                    <button id="btn-draw-gacha-1" onclick="startGachaAnim(1)" class="game-btn flex-1 py-3 bg-gradient-to-b from-blue-300 to-blue-600 rounded-xl font-black text-white border-2 border-white flex flex-col justify-center items-center gap-1 shadow-lg">
                        <span class="text-sm">1回引く</span>
                        <div class="flex items-center gap-1 bg-black/40 px-2 py-0.5 rounded-full"><span class="inline-flex w-4 h-4 relative"><img src="草核.png" class="absolute inset-0 w-full h-full object-contain z-10" onerror="fb(this,'✨','text-xs')"></span><span class="text-white text-xs">1</span></div>
                    </button>
                    <button id="btn-draw-gacha-10" onclick="startGachaAnim(10)" class="game-btn flex-[1.5] py-3 bg-gradient-to-b from-yellow-300 to-orange-500 rounded-xl font-black text-indigo-950 border-2 border-white flex flex-col justify-center items-center gap-1 shadow-lg">
                        <span class="text-lg">10回 連続!!</span>
                        <div class="flex items-center gap-1 bg-black/40 px-2 py-0.5 rounded-full"><span class="inline-flex w-5 h-5 relative"><img src="草核.png" class="absolute inset-0 w-full h-full object-contain z-10" onerror="fb(this,'✨','text-sm')"></span><span class="text-white text-xs">10</span></div>
                    </button>
                </div>
            </div>

            <!-- 5. フレンド画面 -->
            <div id="screen-friend" class="screen hidden p-4 flex flex-col gap-4">
                <div class="bg-indigo-900 rounded-xl p-3 border-2 border-indigo-700 shadow-md text-center flex-shrink-0">
                    <h2 class="text-xl font-black text-transparent bg-clip-text bg-gradient-to-r from-pink-300 to-rose-300">🤝 海賊団（フレンド）</h2>
                    <p class="text-xs text-indigo-300 mt-1 font-bold">仲間を集めて一緒に海を渡ろう！</p>
                </div>
                <div class="bg-indigo-900/80 p-4 rounded-xl border-2 border-indigo-700 shadow-lg text-center relative">
                    <p class="text-xs text-indigo-300 font-bold mb-2">キミの海賊ID（フレンドコード）</p>
                    <div class="flex justify-center items-center gap-2">
                        <span id="my-friend-id" class="text-xl font-black text-yellow-300 tracking-widest bg-indigo-950 px-4 py-2 rounded-lg border border-indigo-500 select-all"></span>
                        <button onclick="copyMyId()" class="game-btn bg-indigo-600 text-white p-2.5 rounded-lg border border-indigo-400">📋</button>
                    </div>
                </div>
                <div class="bg-indigo-900/80 p-3 rounded-xl border-2 border-indigo-700 shadow-lg flex items-center gap-2">
                    <input type="text" id="friend-search-input" placeholder="友達のIDを入力" class="flex-1 bg-indigo-950 border border-indigo-500 rounded-lg p-3 text-white font-bold outline-none uppercase">
                    <button onclick="sendFriendRequest()" class="game-btn bg-pink-500 hover:bg-pink-600 text-white font-black px-4 py-3 rounded-lg border-2 border-white shadow-md whitespace-nowrap">申請!</button>
                </div>
                <div class="flex gap-2">
                    <button onclick="switchFriendTab('list')" id="tab-friend-list" class="flex-1 py-3 bg-indigo-600 text-white font-black rounded-lg border-b-4 border-indigo-800 text-sm transition-all shadow-md">フレンド一覧</button>
                    <button onclick="switchFriendTab('req')" id="tab-friend-req" class="flex-1 py-3 bg-indigo-950 text-indigo-400 font-black rounded-lg border-b-4 border-transparent text-sm transition-all relative shadow-md">
                        届いた申請 <span id="friend-req-badge" class="absolute -top-2 -right-2 bg-red-500 text-white text-[11px] px-2 py-0.5 rounded-full font-black shadow-md border border-white hidden">!</span>
                    </button>
                </div>
                <div id="friend-list-container" class="flex-1 overflow-y-auto no-scrollbar flex flex-col gap-3 pb-8"></div>
            </div>

            <!-- 6. ゲーム画面（Canvas） -->
            <div id="screen-game" class="screen hidden w-full h-full relative">
                
                <!-- チュートリアルガイド -->
                <div id="game-tutorial-overlay" class="absolute top-20 left-4 right-4 pointer-events-none hidden z-30 transition-opacity duration-1000">
                    <div class="bg-indigo-900/95 border-2 border-yellow-400 rounded-2xl p-3 text-center shadow-[0_4px_20px_rgba(0,0,0,0.5)]">
                        <span class="text-yellow-400 font-black text-sm block mb-0.5">💡 ナビーのアドバイス！</span>
                        <p id="game-tutorial-text" class="text-white text-xs leading-relaxed font-bold"></p>
                    </div>
                </div>
                
                <!-- プレイヤーHP表示 -->
                <div id="player-hp-container" class="absolute top-16 left-3 z-10 hidden">
                    <div class="text-[10px] text-green-300 font-black mb-0.5 drop-shadow-md">🚢 船の耐久力</div>
                    <div class="w-32 bg-indigo-950 h-4 rounded-full border-2 border-indigo-500 overflow-hidden shadow-inner relative">
                        <div id="player-hp-bar" class="bg-gradient-to-r from-green-500 to-emerald-400 h-full w-full transition-all duration-200"></div>
                        <span id="player-hp-text" class="absolute inset-0 flex justify-center items-center text-[10px] font-black text-white">5/5</span>
                    </div>
                </div>

                <!-- スコア・撤退UI -->
                <div id="game-ui" class="absolute top-3 left-3 right-3 flex justify-between pointer-events-none z-10">
                    <div class="bg-indigo-950/90 border-2 border-indigo-500 px-4 py-2 rounded-xl text-white font-black shadow-lg flex items-center gap-2">
                        <span class="text-xs text-indigo-300">追い払った数:</span>
                        <span id="game-score" class="text-yellow-400 text-xl drop-shadow-md">0</span> 
                        <span class="text-indigo-500 text-sm">/</span> 
                        <span id="game-goal" class="text-sm">100</span>
                    </div>
                    <button id="btn-retreat" onclick="checkStageClear(true)" class="game-btn pointer-events-auto bg-gradient-to-b from-red-500 to-red-700 border-2 border-white px-5 py-2 rounded-xl text-white font-black shadow-lg text-sm">逃げる!! 🏃</button>
                </div>

                <!-- 💥 必殺技UI 💥 -->
                <div id="special-ui-container" class="absolute bottom-6 right-4 z-10 flex flex-col items-end gap-1 pointer-events-auto hidden">
                    <div class="text-[10px] font-black text-cyan-300 drop-shadow-md tracking-widest">SPECIAL</div>
                    <button id="btn-special" onclick="triggerSpecialSkill()" class="game-btn bg-slate-700 w-16 h-16 rounded-full border-4 border-slate-500 flex items-center justify-center shadow-[0_0_15px_rgba(0,0,0,0.8)] transition-all opacity-80 cursor-not-allowed">
                        <span id="btn-special-icon" class="text-3xl filter grayscale">⚡</span>
                    </button>
                    <div class="w-16 bg-indigo-950 h-3 rounded-full border-2 border-indigo-700 overflow-hidden mt-1 shadow-inner">
                        <div id="special-gauge" class="bg-gradient-to-r from-cyan-400 to-blue-400 h-full w-[0%] transition-all duration-100"></div>
                    </div>
                </div>

                <canvas id="gameCanvas"></canvas>
                
                <div id="defense-line-warning" class="absolute bottom-[60px] w-full text-center text-red-400 text-xs font-black tracking-widest animate-pulse pointer-events-none opacity-50">▼ 防衛ライン（突破されるとGAME OVER）▼</div>

                <!-- 💥 必殺技カットイン演出 💥 -->
                <div id="special-cutin" class="absolute inset-0 bg-indigo-950/80 hidden z-40 overflow-hidden cursor-pointer backdrop-blur-sm" onclick="skipSpecial()">
                    <div id="cutin-flash" class="absolute inset-0 bg-white opacity-0 z-0"></div>
                    <div class="absolute inset-0 bg-[radial-gradient(ellipse_at_center,_var(--tw-gradient-stops))] from-blue-600/30 via-transparent to-transparent opacity-80 z-0"></div>
                    <div id="cutin-char" class="absolute top-1/2 left-[-100%] transform -translate-y-1/2 flex items-center justify-center w-64 h-64 z-10"></div>
                    <div id="cutin-name" class="absolute top-[70%] right-[-100%] transform -translate-y-1/2 text-4xl sm:text-5xl font-black text-transparent bg-clip-text bg-gradient-to-b from-cyan-100 to-blue-500 drop-shadow-[0_0_15px_rgba(0,150,255,1)] italic tracking-widest whitespace-nowrap z-10"></div>
                    <div class="absolute bottom-6 w-full text-center text-white/50 text-xs tracking-widest animate-pulse pointer-events-none z-10">▶ 画面をタップしてスキップ</div>
                </div>

                <!-- ゲーム終了モーダル -->
                <div id="game-end-modal" class="absolute inset-0 bg-indigo-950/95 hidden flex-col items-center justify-center z-20 backdrop-blur-md p-6 overflow-y-auto">
                    <div id="end-confetti-container" class="absolute inset-0 pointer-events-none z-0"></div>
                    <h2 id="game-end-title" class="text-4xl font-black mb-4 text-center text-transparent bg-clip-text bg-gradient-to-b from-yellow-200 to-yellow-500 filter drop-shadow-lg transform scale-110 pop-in relative z-10"></h2>
                    <div class="bg-indigo-900 border-2 border-indigo-500 rounded-2xl p-6 w-full max-w-sm mb-6 shadow-2xl pop-in relative z-10" style="animation-delay: 0.2s; animation-fill-mode: both;">
                        <p id="game-end-desc" class="text-white font-bold text-center text-sm leading-relaxed whitespace-pre-line"></p>
                    </div>
                    <button id="btn-tutorial-exit" onclick="navTo('screen-home')" class="game-btn w-full max-w-[240px] py-4 bg-gradient-to-b from-blue-400 to-blue-600 rounded-full font-black text-white shadow-xl border-2 border-white text-xl pop-in relative z-10" style="animation-delay: 0.4s; animation-fill-mode: both;">港へ帰る！ ⚓</button>
                </div>
            </div>

        </main>

        <!-- 下部ナビ -->
        <nav id="app-nav" class="bg-indigo-950 border-t-4 border-indigo-900 flex justify-between px-2 py-2 pb-6 absolute bottom-0 w-full z-20 shadow-[0_-10px_20px_rgba(0,0,0,0.5)]">
            <button id="nav-btn-home" onclick="navTo('screen-home')" class="flex-1 nav-btn flex flex-col items-center p-2 text-indigo-400 hover:text-white transition-transform transform hover:-translate-y-2" data-target="screen-home">
                <span class="text-3xl mb-1 filter drop-shadow-md">🏠</span><span class="text-[11px] font-black">ホーム</span>
            </button>
            <button id="nav-btn-equip" onclick="tryAccessLockedScreen('screen-equip')" class="flex-1 nav-btn flex flex-col items-center p-2 text-indigo-400 hover:text-white transition-transform transform hover:-translate-y-2 relative" data-target="screen-equip">
                <span id="nav-equip-lock" class="absolute top-1 right-2 text-xs bg-red-600 text-white rounded-full px-1.5 py-0.5 shadow-md font-bold">🔒</span>
                <span class="text-3xl mb-1 filter drop-shadow-md">⚓</span><span class="text-[11px] font-black">準備・強化</span>
            </button>
            <button id="nav-btn-gacha" onclick="tryAccessLockedScreen('screen-gacha')" class="flex-1 nav-btn flex flex-col items-center p-2 text-indigo-400 hover:text-white transition-transform transform hover:-translate-y-2 relative" data-target="screen-gacha">
                <span id="nav-gacha-lock" class="absolute top-1 right-2 text-xs bg-red-600 text-white rounded-full px-1.5 py-0.5 shadow-md font-bold">🔒</span>
                <span class="text-3xl mb-1 filter drop-shadow-md">🎁</span><span class="text-[11px] font-black">宝箱</span>
            </button>
            <button id="nav-btn-friend" onclick="tryAccessLockedScreen('screen-friend')" class="flex-1 nav-btn flex flex-col items-center p-2 text-indigo-400 hover:text-white transition-transform transform hover:-translate-y-2 relative" data-target="screen-friend">
                <span id="nav-friend-lock" class="absolute top-1 right-2 text-xs bg-red-600 text-white rounded-full px-1.5 py-0.5 shadow-md font-bold">🔒</span>
                <span class="text-3xl mb-1 filter drop-shadow-md">🤝</span><span class="text-[11px] font-black">海賊団</span>
            </button>
        </nav>

        <!-- モーダル類 -->
        
        <!-- マルチプレイ（フレンド選択）モーダル -->
        <div id="friend-select-modal" class="absolute inset-0 bg-indigo-950/90 hidden flex-col items-center justify-center z-40 p-4 backdrop-blur-sm">
            <div class="bg-indigo-900 border-4 border-indigo-500 rounded-3xl p-5 w-full max-w-[340px] shadow-2xl flex flex-col gap-4 pop-in relative h-[75vh]">
                <button onclick="closeFriendSelectModal()" class="absolute top-3 right-3 w-8 h-8 bg-indigo-950 border border-indigo-700 rounded-full text-white font-black flex justify-center items-center active:scale-95 z-20">✕</button>
                <h3 id="friend-select-title" class="text-xl font-black text-white text-center border-b-2 border-indigo-700 pb-2">出航するフレンドを選択</h3>
                <p id="friend-select-desc" class="text-xs text-indigo-300 text-center font-bold">選んだ仲間と一緒に出航するぞ！</p>
                <div id="friend-select-list" class="flex-1 overflow-y-auto no-scrollbar flex flex-col gap-3 pb-4">
                    <!-- JSで動的に追加 -->
                </div>
            </div>
        </div>

        <div id="tutorial-overlay" class="absolute inset-0 bg-indigo-950/80 hidden flex-col items-center justify-center z-50 p-6 backdrop-blur-sm">
            <div class="bg-indigo-900 border-4 border-yellow-400 rounded-3xl p-6 w-full max-w-[340px] shadow-2xl flex flex-col gap-5 pop-in">
                <div class="flex justify-between items-center border-b-2 border-indigo-700 pb-3">
                    <span class="text-yellow-400 font-black tracking-widest text-sm flex items-center gap-1"><span>💡</span> ナビーのわくわくガイド</span>
                    <span id="tutorial-step-counter" class="text-xs text-indigo-300 font-bold bg-indigo-950 px-2 py-1 rounded-full"></span>
                </div>
                <div class="text-center py-2 flex flex-col items-center">
                    <span id="tutorial-character" class="text-6xl mb-4 filter drop-shadow-lg animate-bounce">🦜</span>
                    <p id="tutorial-text" class="text-sm text-white font-bold leading-relaxed whitespace-pre-line bg-indigo-950/50 p-4 rounded-xl border border-indigo-800"></p>
                </div>
                <button onclick="nextTutorialStep()" class="game-btn w-full py-4 bg-gradient-to-b from-yellow-300 to-orange-500 text-indigo-950 font-black text-lg rounded-2xl shadow-lg border-2 border-white">OK！わかった！ 👍</button>
            </div>
        </div>
        <div id="tutorial-arrow" class="absolute hidden z-50 pointer-events-none bounce-arrow"><span class="text-6xl text-yellow-400 drop-shadow-[0_5px_15px_rgba(234,179,8,1)]">👇</span></div>
        <div id="custom-alert-modal" class="absolute inset-0 bg-indigo-950/90 hidden flex-col items-center justify-center z-[9999] p-4 backdrop-blur-sm">
            <div class="bg-indigo-900 border-4 border-yellow-400 rounded-3xl p-6 w-full max-w-[320px] shadow-2xl flex flex-col gap-4 pop-in text-center">
                <span class="text-6xl mb-2 animate-bounce drop-shadow-md">🦜</span>
                <p id="custom-alert-text" class="text-white font-bold leading-relaxed whitespace-pre-line bg-indigo-950/50 p-4 rounded-xl border border-indigo-700"></p>
                <button onclick="closeCustomAlert()" class="game-btn w-full py-4 mt-2 bg-gradient-to-b from-yellow-300 to-orange-500 text-indigo-950 font-black text-lg rounded-xl shadow-lg border-2 border-white">わかった！ 👍</button>
            </div>
        </div>
        
        <!-- スタミナ回復モーダル -->
        <div id="stamina-modal" class="absolute inset-0 bg-indigo-950/90 hidden flex-col items-center justify-center z-50 p-4 backdrop-blur-sm">
            <div class="bg-indigo-900 border-4 border-yellow-400 rounded-3xl p-5 w-full max-w-[320px] shadow-2xl flex flex-col gap-4 pop-in relative text-center">
                <button onclick="closeStaminaModal()" class="absolute top-3 right-3 w-8 h-8 bg-indigo-950 border border-indigo-700 rounded-full text-white font-black flex justify-center items-center active:scale-95 z-20">✕</button>
                <h3 class="text-xl font-black text-transparent bg-clip-text bg-gradient-to-r from-yellow-300 to-orange-400 tracking-wider border-b-2 border-indigo-700 pb-2 mb-1">⚡ スタミナ回復</h3>
                <p class="text-sm text-white font-bold mb-1">草コインを使って、<br>すぐにスタミナを回復するぞ！</p>
                <div class="bg-indigo-950 p-4 rounded-xl border border-indigo-700 flex flex-col items-center gap-3 shadow-inner">
                    <div class="text-2xl font-black text-yellow-300">スタミナ + 50</div>
                    <button onclick="buyStamina()" class="game-btn w-full flex items-center justify-center gap-2 px-4 py-3 bg-gradient-to-b from-green-400 to-green-600 rounded-full font-black text-white border-2 border-white shadow-md text-lg">
                        <span class="inline-flex relative w-6 h-6"><img src="草コイン.png" class="absolute inset-0 w-full h-full object-contain" onerror="fb(this,'💰')"></span>
                        <span>500 で回復！</span>
                    </button>
                </div>
            </div>
        </div>

        <div id="locked-alert-overlay" class="absolute inset-0 bg-indigo-950/80 hidden flex-col items-center justify-center z-50 p-6 backdrop-blur-sm">
            <div class="bg-indigo-900 border-4 border-red-500 rounded-3xl p-6 w-full max-w-[320px] shadow-2xl flex flex-col gap-4 pop-in">
                <div class="flex justify-between items-center border-b-2 border-indigo-700 pb-2"><span class="text-red-400 font-black tracking-wider text-sm flex items-center gap-1"><span>🔒</span> おっと待って！</span></div>
                <div class="text-center py-2 flex flex-col items-center">
                    <span class="text-6xl mb-3 animate-pulse">🛑</span>
                    <p class="text-sm text-white font-bold leading-relaxed whitespace-pre-line bg-indigo-950/50 p-4 rounded-xl">新米船長！ここはまだヒミツの場所だ！\nまずは「大冒険へ出発!!」を押して、最初の冒険をクリアしてくれ！</p>
                </div>
                <button onclick="closeLockedAlert()" class="game-btn w-full py-3 bg-slate-600 text-white font-black rounded-xl shadow-lg border-2 border-slate-400 text-lg">はーい！ 🏃</button>
            </div>
        </div>
        
        <!-- チュートリアル完了時モーダル -->
        <div id="tutorial-clear-modal" class="absolute inset-0 bg-indigo-950/95 hidden flex-col items-center justify-center z-50 p-6 overflow-hidden">
            <div class="holy-aura"></div><div id="tutorial-confetti-container" class="absolute inset-0 pointer-events-none"></div>
            <div class="relative z-10 text-center flex flex-col items-center gap-6 pop-in">
                <div class="text-7xl animate-bounce filter drop-shadow-[0_0_20px_rgba(250,204,21,0.8)]">👑</div>
                <h2 class="text-4xl font-black text-transparent bg-clip-text bg-gradient-to-r from-yellow-200 via-orange-400 to-yellow-500 tracking-widest drop-shadow-lg">ライセンス獲得!!</h2>
                <div class="bg-indigo-900/90 border-2 border-yellow-500 rounded-2xl p-5 w-full max-w-xs text-sm font-bold text-white leading-relaxed shadow-2xl backdrop-blur">
                    大・大・大成功！！<br>キミはもう立派な船長だ！！<br><br>
                    <span class="text-yellow-400 text-lg block mb-1">🔓「編成・準備」</span>
                    <span class="text-yellow-400 text-lg block mb-1">🔓「伝説の宝箱」</span>
                    <span class="text-yellow-400 text-lg block">🔓「海賊団 (フレンド)」</span><br>が、あそべるようになったぞ！
                </div>
                <div class="bg-indigo-800/90 p-4 rounded-2xl border-2 border-indigo-500 w-full flex flex-col items-center gap-2 shadow-inner">
                    <span class="text-xs text-yellow-300 font-black">🎉 船長就任の超・特大プレゼント！ 🎉</span>
                    <div class="flex justify-center gap-3 w-full">
                        <div class="flex flex-col items-center bg-indigo-950 px-3 py-2 rounded-xl border border-indigo-700 w-1/2">
                            <span class="inline-flex relative w-6 h-6 mb-1"><img src="草核.png" class="absolute inset-0 w-full h-full object-contain" onerror="fb(this,'✨')"></span><span class="text-yellow-400 text-xl font-black">× 10</span>
                        </div>
                        <div class="flex flex-col items-center bg-indigo-950 px-3 py-2 rounded-xl border border-indigo-700 w-1/2">
                            <span class="inline-flex relative w-6 h-6 mb-1"><img src="草コイン.png" class="absolute inset-0 w-full h-full object-contain" onerror="fb(this,'💰')"></span><span class="text-yellow-400 text-xl font-black">× 2000</span>
                        </div>
                    </div>
                </div>
                <button onclick="finishTutorialClear()" class="game-btn w-full max-w-[260px] py-5 bg-gradient-to-b from-yellow-300 to-orange-500 text-indigo-950 font-black rounded-full shadow-2xl border-4 border-white text-xl mt-2">最高だぜ！！ 🚀</button>
            </div>
        </div>

        <div id="profile-modal" class="absolute inset-0 bg-indigo-950/90 hidden flex-col items-center justify-center z-40 p-4 backdrop-blur-sm">
            <div class="bg-indigo-900 p-6 rounded-3xl w-full max-w-[320px] border-4 border-indigo-700 shadow-2xl pop-in">
                <h3 class="text-xl font-black mb-5 text-center border-b-2 border-indigo-700 pb-3 text-white">🏴‍☠️ 船長プロフィール</h3>
                <div class="mb-5">
                    <label class="block text-xs text-indigo-300 font-bold mb-2">好きなアイコンを選ぼう！</label>
                    <div class="flex justify-between bg-indigo-950 p-3 rounded-2xl border-2 border-indigo-800 shadow-inner">
                        <button class="icon-sel-btn text-3xl p-2 rounded-xl transition-transform hover:scale-110 bg-indigo-700 border-2 border-blue-400" onclick="selectIcon('👤')">👤</button>
                        <button class="icon-sel-btn text-3xl p-2 rounded-xl transition-transform hover:scale-110" onclick="selectIcon('🧑‍🦲')">🧑‍🦲</button>
                        <button class="icon-sel-btn text-3xl p-2 rounded-xl transition-transform hover:scale-110" onclick="selectIcon('💀')">💀</button>
                        <button class="icon-sel-btn text-3xl p-2 rounded-xl transition-transform hover:scale-110" onclick="selectIcon('🦜')">🦜</button>
                    </div>
                    <div class="mt-3 flex flex-col items-center gap-2">
                        <input type="file" id="icon-upload" accept="image/*" class="hidden" onchange="handleIconUpload(event)">
                        <button onclick="document.getElementById('icon-upload').click()" class="game-btn bg-indigo-600 text-white px-3 py-2 rounded-xl text-xs font-bold border border-indigo-400 flex items-center gap-1"><span>📷</span> 自分の画像を使う</button>
                        <div id="custom-icon-preview-container" class="hidden mt-1 flex justify-center">
                            <button id="custom-icon-btn" class="icon-sel-btn w-12 h-12 rounded-xl transition-transform hover:scale-110 bg-indigo-950 border-2 border-indigo-800 overflow-hidden flex items-center justify-center p-0" onclick="selectIcon('CUSTOM_IMAGE')"><img id="custom-icon-img" src="" class="w-full h-full object-cover"></button>
                        </div>
                    </div>
                </div>
                <div class="mb-8">
                    <label class="block text-xs text-indigo-300 font-bold mb-2">船長の名前（カッコよくね！）</label>
                    <input type="text" id="edit-username" class="w-full bg-indigo-950 border-2 border-indigo-700 rounded-xl p-3 text-white font-black text-lg outline-none focus:border-yellow-400 shadow-inner" maxlength="8" placeholder="なまえをいれてね">
                </div>
                <div class="flex gap-3">
                    <button onclick="closeProfileModal()" class="game-btn flex-1 py-3 bg-slate-600 border-2 border-slate-400 text-white rounded-xl font-black text-lg">やめる</button>
                    <button onclick="saveProfile()" class="game-btn flex-1 py-3 bg-gradient-to-b from-blue-400 to-blue-600 border-2 border-white text-white rounded-xl font-black text-lg shadow-lg">けってい！</button>
                </div>
            </div>
        </div>

        <div id="login-bonus-modal" class="absolute inset-0 bg-indigo-950/95 hidden flex-col items-center justify-center z-50 p-6 overflow-hidden">
            <div class="holy-aura"></div><div id="login-confetti-container" class="absolute inset-0 pointer-events-none z-0"></div>
            <div class="relative z-10 text-center flex flex-col items-center gap-5 pop-in">
                <div class="text-7xl animate-bounce filter drop-shadow-[0_0_20px_rgba(250,204,21,0.8)]">🎁</div>
                <h2 class="text-3xl font-black text-transparent bg-clip-text bg-gradient-to-r from-yellow-200 via-orange-400 to-yellow-500 tracking-wider drop-shadow-lg">ログインボーナス!!</h2>
                <p class="text-indigo-200 text-xs font-bold">毎日港に寄港して、豪華なお祝いを手に入れよう！</p>
                <div id="login-grid" class="grid grid-cols-4 gap-2 w-full max-w-sm bg-indigo-900/80 p-3 rounded-2xl border-2 border-indigo-700 shadow-inner"></div>
                <div class="bg-indigo-800/90 border-2 border-yellow-500 rounded-2xl p-4 w-full max-w-xs text-center backdrop-blur shadow-xl">
                    <span class="text-xs text-yellow-300 font-black block mb-1">🎁 本日の獲得プレゼント 🎁</span>
                    <div id="login-today-reward-visual" class="flex items-center justify-center gap-2 bg-indigo-950 px-4 py-2 rounded-full border border-indigo-700 w-fit mx-auto shadow-inner"></div>
                </div>
                <button onclick="claimLoginBonus()" class="game-btn w-full max-w-[240px] py-4 bg-gradient-to-b from-yellow-300 to-orange-500 text-indigo-950 font-black rounded-full shadow-2xl border-4 border-white text-xl">ありがたく受け取る！ 👍</button>
            </div>
        </div>

        <div id="gacha-probability-modal" class="absolute inset-0 bg-indigo-950/95 hidden flex-col items-center justify-start z-50 p-4 overflow-y-auto no-scrollbar">
            <div class="w-full max-w-md bg-indigo-900 border-4 border-indigo-700 rounded-3xl p-5 shadow-2xl flex flex-col gap-4 relative mt-4 mb-8">
                <button onclick="closeGachaProbabilityModal()" class="absolute top-3 right-3 w-8 h-8 bg-indigo-950 border border-indigo-700 rounded-full text-white font-black flex justify-center items-center active:scale-95 z-20">✕</button>
                <h3 class="text-2xl font-black text-transparent bg-clip-text bg-gradient-to-r from-yellow-300 to-amber-300 tracking-wider border-b-2 border-indigo-700 pb-2">📋 提供割合と一覧</h3>
                <div class="bg-indigo-950 p-3 rounded-xl border border-indigo-800 shadow-inner">
                    <h4 class="text-sm font-black text-yellow-400 mb-1">⭐ レアリティ排出確率</h4>
                    <div class="grid grid-cols-2 gap-2 text-xs font-bold text-slate-300">
                        <div class="flex justify-between border-b border-indigo-800/50 pb-1"><span class="text-pink-400">超激レア (UR):</span> <span class="text-white font-black">1.0%〜 (爆死救済)</span></div>
                        <div class="flex justify-between items-center border-b border-indigo-800/50 pb-1"><span class="text-orange-400">激レア (SSR):</span> <span class="text-white">4.0%</span></div>
                        <div class="flex justify-between items-center border-b border-indigo-800 pb-1"><span class="text-purple-400">レア (SR):</span> <span class="text-white">10.0%</span></div>
                        <div class="flex justify-between items-center"><span class="text-blue-400">ちょっとレア (R):</span> <span class="text-white">30.0%</span></div>
                        <div class="flex justify-between"><span>ノーマル (N):</span> <span class="text-white">55.0%</span></div>
                    </div>
                </div>
                <div class="space-y-3 overflow-y-auto max-h-[300px] pr-1">
                    <div>
                        <h4 class="text-xs font-black text-pink-400 bg-indigo-950 px-2 py-1 rounded mb-1">【UR】超激レア（大当り）</h4>
                        <div class="text-xs space-y-1 pl-2 font-bold text-slate-200">
                            <div class="flex justify-between"><span>海神の光条 (大砲)</span><span class="text-yellow-400">通常のみ</span></div>
                            <div class="flex justify-between"><span>北前船🎌 (船スキン)</span><span class="text-yellow-400">通常のみ</span></div>
                            <div class="flex justify-between"><span>伝説のキャプテン (乗組員)</span><span class="text-yellow-400">通常のみ</span></div>
                            <div class="flex justify-between text-yellow-300"><span>雷戦艦せいまろ号⚡🚢 (船)</span><span>EX限定!!</span></div>
                            <div class="flex justify-between text-yellow-300"><span>せいまろ🐾 (限定乗組員)</span><span>EX限定!!</span></div>
                        </div>
                    </div>
                    <div>
                        <h4 class="text-xs font-black text-orange-400 bg-indigo-950 px-2 py-1 rounded mb-1">【SSR】激レア</h4>
                        <div class="text-xs space-y-1 pl-2 font-bold text-slate-200">
                            <div class="flex justify-between"><span>タイタニック🚢 (船スキン)</span><span>通常＆EX</span></div>
                            <div class="flex justify-between"><span>ヤマト⛴️ (船スキン)</span><span>通常＆EX</span></div>
                            <div class="flex justify-between"><span>グランドタイタス🛳️ (船スキン)</span><span>通常＆EX</span></div>
                        </div>
                    </div>
                    <div>
                        <h4 class="text-xs font-black text-purple-400 bg-indigo-950 px-2 py-1 rounded mb-1">【SR】レア</h4>
                        <div class="text-xs space-y-1 pl-2 font-bold text-slate-200">
                            <div class="flex justify-between"><span>ハイスピードホース (大砲)</span><span>通常＆EX</span></div>
                            <div class="flex justify-between"><span>木の船🛶 (船スキン)</span><span>通常＆EX</span></div>
                            <div class="flex justify-between"><span>鉄の船🛳️ (船スキン)</span><span>通常＆EX</span></div>
                            <div class="flex justify-between"><span>弁才船⛵ (船スキン)</span><span>通常＆EX</span></div>
                            <div class="flex justify-between"><span>菱垣廻船🛥️ (船スキン)</span><span>通常＆EX</span></div>
                            <div class="flex justify-between"><span>天才ナビゲーター🗺️ (乗組員)</span><span>通常＆EX</span></div>
                        </div>
                    </div>
                </div>
                <div class="text-[9px] text-center text-indigo-300 border-t border-indigo-700 pt-2 font-bold">※ダブりは自動的に「草コイン×500」と「草の化石×1」に変換されます！</div>
            </div>
        </div>

        <div id="item-detail-modal" class="absolute inset-0 bg-indigo-950/90 hidden flex-col items-center justify-center z-50 p-4 backdrop-blur-sm">
            <div class="bg-indigo-900 border-4 border-indigo-500 rounded-3xl p-5 w-full max-w-[320px] shadow-2xl flex flex-col gap-4 pop-in relative">
                <button onclick="closeItemDetail()" class="absolute top-3 right-3 w-8 h-8 bg-indigo-950 border border-indigo-700 rounded-full text-white font-black flex justify-center items-center active:scale-95 z-20">✕</button>
                <div class="flex justify-between items-center border-b-2 border-indigo-700 pb-2 pr-8">
                    <span id="detail-rarity" class="font-black text-lg"></span><span id="detail-level" class="text-yellow-300 font-black text-lg drop-shadow-md">Lv.1</span>
                </div>
                <div class="flex flex-col items-center py-2">
                    <div id="detail-icon" class="text-6xl mb-3 filter drop-shadow-md flex items-center justify-center h-20 w-20"></div>
                    <h4 id="detail-name" class="text-2xl font-black text-white text-center"></h4>
                    <p id="detail-desc" class="text-sm text-indigo-200 mt-2 text-center font-bold bg-indigo-950/50 p-2 rounded-xl w-full whitespace-pre-line"></p>
                </div>
                <div class="bg-indigo-950/80 p-3 rounded-xl border border-indigo-700 flex flex-col gap-2 shadow-inner">
                    <div class="text-xs text-indigo-300 font-bold text-center mb-1">⬆️ レベルアップに必要な素材 ⬆️</div>
                    <div class="flex justify-around items-center">
                        <div class="flex items-center gap-1.5">
                            <span class="inline-flex relative w-6 h-6"><img src="草コイン.png" class="absolute inset-0 w-full h-full object-contain" onerror="fb(this,'💰')"></span><span id="req-coins" class="text-white font-black text-lg">100</span>
                        </div>
                        <span class="text-indigo-500 font-black">+</span>
                        <div class="flex items-center gap-1.5">
                            <span class="inline-flex relative w-6 h-6"><img src="草の化石.png" class="absolute inset-0 w-full h-full object-contain" onerror="fb(this,'🦴')"></span><span id="req-materials" class="text-white font-black text-lg">1</span>
                        </div>
                    </div>
                    <button id="btn-upgrade" onclick="upgradeItem()" class="game-btn w-full py-3 mt-2 bg-gradient-to-b from-green-400 to-green-600 rounded-xl font-black text-white border-2 border-white shadow-md text-lg">パワーアップ!! ✨</button>
                </div>
                <button id="btn-equip" onclick="equipItem()" class="game-btn w-full py-4 mt-1 bg-gradient-to-b from-blue-400 to-blue-600 rounded-xl font-black text-white border-2 border-white text-xl shadow-lg">装備する！ ⚓</button>
            </div>
        </div>

        <div id="gacha-anim-container" class="absolute inset-0 bg-indigo-950/95 hidden flex-col items-center justify-center z-40 overflow-hidden">
            <div id="ur-anim-layer" class="absolute inset-0 w-[200%] h-[200%] left-[-50%] top-[-50%]"></div>
            <div id="gacha-chest" class="text-[9rem] relative z-10 transition-transform drop-shadow-[0_20px_30px_rgba(255,255,0,0.8)] filter">📦</div>
            <div id="ur-flash-text" class="absolute top-1/4 w-full text-center hidden z-20"><div class="text-7xl font-black text-white ur-text-flash tracking-widest italic drop-shadow-2xl">超・大当り!!</div></div>
        </div>

        <div id="gacha-result-modal" class="absolute inset-0 bg-indigo-950/95 hidden flex-col items-center justify-center z-50 p-6 overflow-hidden">
            <div id="gacha-confetti-container" class="absolute inset-0 pointer-events-none z-0"></div>
            <h3 id="gacha-result-rarity" class="text-6xl font-black mb-4 gacha-result-text italic tracking-wider drop-shadow-lg relative z-10"></h3>
            <div id="gacha-result-visual-container" class="w-32 h-32 flex items-center justify-center mb-6 gacha-result-text relative z-10 bg-indigo-900/50 rounded-full shadow-[0_0_50px_rgba(255,255,255,0.2)]"></div>
            <div class="bg-indigo-900 border-2 border-indigo-500 p-5 rounded-2xl w-full max-w-sm mb-8 gacha-result-text shadow-2xl relative z-10 backdrop-blur-sm">
                <h4 id="gacha-result-name" class="text-3xl font-black text-white mb-3 text-center drop-shadow-md"></h4>
                <p id="gacha-result-desc" class="text-sm text-indigo-200 text-center font-bold leading-relaxed bg-indigo-950/50 p-3 rounded-xl whitespace-pre-line"></p>
            </div>
            <button onclick="closeGachaResult()" class="game-btn px-12 py-5 bg-gradient-to-b from-yellow-300 to-orange-500 rounded-full font-black w-full max-w-[240px] shadow-xl border-4 border-white text-xl text-indigo-950 relative z-10">ヤッター！！ 🎉</button>
        </div>

        <div id="gacha-result-10-modal" class="absolute inset-0 bg-indigo-950/95 hidden flex-col items-center justify-start z-50 p-4 overflow-y-auto no-scrollbar">
            <div id="gacha-confetti-container-10" class="absolute inset-0 pointer-events-none z-0"></div>
            <h3 class="text-3xl font-black mt-4 mb-4 text-transparent bg-clip-text bg-gradient-to-r from-yellow-200 to-orange-400 italic tracking-wider drop-shadow-lg relative z-10 text-center">10連ガチャ結果!!</h3>
            <div id="gacha-10-grid" class="grid grid-cols-2 sm:grid-cols-3 gap-3 w-full max-w-sm mb-6 relative z-10"></div>
            <div class="w-full flex justify-center pb-8 mt-auto"><button onclick="closeGacha10Result()" class="game-btn px-10 py-4 bg-gradient-to-b from-blue-400 to-blue-600 rounded-full font-black w-full max-w-[240px] shadow-xl border-4 border-white text-xl text-white relative z-10">すべて確認！</button></div>
        </div>

        <div id="happy-share-toast" class="bg-gradient-to-r from-pink-500 via-red-500 to-yellow-500 border-4 border-white shadow-[0_10px_30px_rgba(250,204,21,0.8)] rounded-2xl p-4 flex items-center gap-4">
            <div class="text-5xl animate-bounce filter drop-shadow-lg select-none">👑</div>
            <div class="flex-1">
                <div class="text-xs text-yellow-100 font-black tracking-widest bg-black/30 px-2 py-0.5 rounded-full inline-block mb-1">🎉 奇跡のおすそわけ！ 🎉</div>
                <div id="toast-message" class="text-base text-white font-black leading-tight drop-shadow-md">〇〇がURを入手！</div>
                <div class="flex items-center gap-2 mt-2 bg-indigo-950/80 rounded-lg px-3 py-1.5 w-fit border border-yellow-500/50 shadow-inner">
                    <span class="text-[10px] text-yellow-300 font-bold">お祝い🎁</span>
                    <span class="inline-flex relative w-4 h-4 ml-1"><img src="草核.png" class="absolute inset-0 w-full h-full object-contain" onerror="fb(this,'✨','text-xs')"></span>
                    <span class="text-xs text-yellow-400 font-black">×1 ゲット！</span>
                </div>
            </div>
            <button onclick="closeHappyShareToast()" class="absolute top-2 right-2 text-white bg-black/40 hover:bg-black/60 rounded-full w-6 h-6 flex items-center justify-center font-bold text-xs shadow-md transition-colors active:scale-95 select-none z-10">✕</button>
        </div>

    </div>

<script>
/**
 * 🎵 オーディオ（サウンド）制御
 */
const clickSe = new Audio('決定ボタンを押す10.mp3');
const homeBgm = new Audio('Pirates.mp3');
homeBgm.loop = true;
homeBgm.volume = 0.4;

let isAudioInitialized = false;

document.addEventListener('click', (e) => {
    if (!isAudioInitialized) {
        clickSe.play().catch(()=>{});
        clickSe.pause();
        clickSe.currentTime = 0;
        if (!document.getElementById('screen-home').classList.contains('hidden')) {
            homeBgm.play().catch(err => console.warn("BGM Play Failed", err));
        }
        isAudioInitialized = true;
    }

    const targetBtn = e.target.closest('button, .icon-sel-btn, .game-btn, .nav-btn');
    if (targetBtn && !targetBtn.disabled) {
        clickSe.currentTime = 0;
        clickSe.play().catch(()=>{});
    }
});

function showAlert(msg) {
    document.getElementById('custom-alert-text').innerText = msg;
    document.getElementById('custom-alert-modal').classList.remove('hidden');
    document.getElementById('custom-alert-modal').classList.add('flex');
}
function closeCustomAlert() {
    document.getElementById('custom-alert-modal').classList.add('hidden');
    document.getElementById('custom-alert-modal').classList.remove('flex');
}

function checkRectCollision(r1, r2) {
    const w1 = r1.w || r1.width; const h1 = r1.h || r1.height;
    const w2 = r2.w || r2.width; const h2 = r2.h || r2.height;
    return r1.x < r2.x + w2 && r1.x + w1 > r2.x && r1.y < r2.y + h2 && r1.y + h1 > r2.y;
}

function createPopParticles(x, y, color, isStar = false) {
    const count = isStar ? 12 : 8;
    for (let i = 0; i < count; i++) {
        gameState.particles.push({
            x: x, y: y, dx: (Math.random() - 0.5) * 12, dy: (Math.random() - 0.5) * 12 - 2, 
            life: 20 + Math.random() * 20, color: isStar && Math.random()>0.5 ? '#fff' : color, 
            size: Math.random() * 6 + 3, isStar: isStar && Math.random() > 0.3 
        });
    }
}

function drawStar(ctx, cx, cy, spikes, outerRadius, innerRadius, color) {
    let rot = Math.PI / 2 * 3; let x = cx; let y = cy; let step = Math.PI / spikes;
    ctx.beginPath(); ctx.moveTo(cx, cy - outerRadius);
    for (let i = 0; i < spikes; i++) {
        x = cx + Math.cos(rot) * outerRadius; y = cy + Math.sin(rot) * outerRadius; ctx.lineTo(x, y); rot += step;
        x = cx + Math.cos(rot) * innerRadius; y = cy + Math.sin(rot) * innerRadius; ctx.lineTo(x, y); rot += step;
    }
    ctx.lineTo(cx, cy - outerRadius); ctx.closePath(); ctx.fillStyle = color; ctx.fill();
}

function drawLightningLine(ctx, startX, startY, endX, endY, color, width, blur, branches = true) {
    ctx.save(); ctx.strokeStyle = color; ctx.lineWidth = width; ctx.shadowColor = '#0284c7'; ctx.shadowBlur = blur; ctx.lineCap = 'round'; ctx.lineJoin = 'round';
    ctx.beginPath(); ctx.moveTo(startX, startY);
    let currentX = startX; let currentY = startY; const distanceY = endY - startY; const segments = 8; 
    for (let i = 1; i <= segments; i++) {
        const targetY = startY + (distanceY / segments) * i;
        const targetX = startX + (endX - startX) * (i / segments) + (Math.random() - 0.5) * 60; 
        ctx.lineTo(targetX, targetY);
        if (branches && Math.random() > 0.6 && i < segments) {
            ctx.save(); ctx.beginPath(); ctx.moveTo(targetX, targetY);
            let branchX = targetX; let branchY = targetY;
            for(let j=0; j<3; j++) { branchX += (Math.random() - 0.5) * 60; branchY += Math.random() * 40; ctx.lineTo(branchX, branchY); }
            ctx.lineWidth = width * 0.4; ctx.stroke(); ctx.restore(); ctx.moveTo(targetX, targetY); 
        }
        currentX = targetX; currentY = targetY;
    }
    ctx.stroke(); ctx.restore();
}

function shootLightningImpactConfetti(x, y) {
    for (let i = 0; i < 6; i++) {
        gameState.particles.push({
            x: x, y: y, dx: (Math.random() - 0.5) * 20, dy: (Math.random() - 0.5) * 20 - 4, 
            life: 15 + Math.random() * 15, color: Math.random() > 0.4 ? '#e0f2fe' : '#38bdf8', 
            size: Math.random() * 8 + 4, isStar: true 
        });
    }
}
</script>

<script>
let DB = {
    userId: 'user_001', userName: '新米船長', userIcon: '👤', gachaTickets: 0, coins: 0, materials: 0,   
    itemLevels: {}, pityCounter: 0, ownedWeapons: ['wp_normal'], ownedSkins: ['skin_default'], ownedCrew: ['crew_none'],
    equippedWeapon: 'wp_normal', equippedSkin: 'skin_default', equippedCrew: 'crew_none', clearedStages: 0, clearedSeimaroStage: 0, 
    tutorialCompleted: false, tutorialGachaCompleted: false, tutorialEquipCompleted: false, lastLoginDate: '', loginStreak: 0, lastShareTimestamp: 0,
    stamina: 100, lastStaminaUpdateTime: Date.now()
};

const LOGIN_BONUS_MASTER = [
    { day: 1, type: 'coins', amount: 500, label: '草コイン', icon: '草コイン.png', fallback: '💰' },
    { day: 2, type: 'materials', amount: 2, label: '草の化石', icon: '草の化石.png', fallback: '🦴' },
    { day: 3, type: 'tickets', amount: 2, label: '草核', icon: '草核.png', fallback: '✨' },
    { day: 4, type: 'coins', amount: 1200, label: '草コイン', icon: '草コイン.png', fallback: '💰' },
    { day: 5, type: 'materials', amount: 5, label: '草の化石', icon: '草の化石.png', fallback: '🦴' },
    { day: 6, type: 'tickets', amount: 3, label: '草核', icon: '草核.png', fallback: '✨' },
    { day: 7, type: 'special', amount: 5, coinsBonus: 3000, label: '超・船長お祝いパック', icon: '草核.png', fallback: '👑' }
];

const MASTER = {
    systems: {
        'stamina_cap': { id: 'stamina_cap', name: 'スタミナタンク', rarity: 'SR', icon: '🔋', fallbackIcon: '🔋', type: 'text', desc: '最大スタミナがアップするぞ！\nレベル1につき上限が+10される！' }
    },
    weapons: {
        'wp_normal': { id: 'wp_normal', name: '木の大砲', rarity: 'N', fireRate: 15, speed: 6, type: 'single', color: '#60a5fa', desc: '普通の弾を撃ち出す大砲だ！' },
        'wp_spread': { id: 'wp_spread', name: '鉄の散弾砲', rarity: 'R', fireRate: 20, speed: 5, type: 'spread', color: '#38bdf8', desc: '3方向に弾をばらまく！当てやすいぞ！' },
        'wp_machine':{ id: 'wp_machine', name: 'ガトリング砲', rarity: 'SR', fireRate: 5, speed: 8, type: 'single', color: '#2dd4bf', desc: '超すごいスピードで弾を連射する大砲だ！' },
        'wp_legend': { id: 'wp_legend', name: '海神の光条', rarity: 'UR', fireRate: 10, speed: 15, type: 'laser', color: '#a78bfa', desc: '全てを貫通する最強のレーザー大砲！！' }
    },
    skins: {
        'skin_default': { id: 'skin_default', name: '丸太 of イカダ', rarity: 'N', icon: '🛶' },
        'skin_pirate': { id: 'skin_pirate', name: 'アドベンチャー船', rarity: 'R', icon: '⛵' },
        'skin_wood': { id: 'skin_wood', name: '木の船', rarity: 'SR', icon: '🛶' },
        'skin_iron': { id: 'skin_iron', name: '鉄の船', rarity: 'SR', icon: '🛳️' },
        'skin_benzai': { id: 'skin_benzai', name: '弁才船', rarity: 'SR', icon: '⛵' },
        'skin_higaki': { id: 'skin_higaki', name: '菱垣廻船', rarity: 'SR', icon: '🛥️' },
        'skin_titanic': { id: 'skin_titanic', name: 'タイタニック', rarity: 'SSR', icon: '🚢' },
        'skin_yamato': { id: 'skin_yamato', name: 'ヤマト', rarity: 'SSR', icon: '⛴️' },
        'skin_titus': { id: 'skin_titus', name: 'グランドタイタス', rarity: 'SSR', icon: '🛳️' },
        'skin_kitamae': { id: 'skin_kitamae', name: '北前船', rarity: 'UR', icon: '🎌' },
        'skin_seimaro_ship': { id: 'skin_seimaro_ship', name: '雷戦艦せいまろ号', rarity: 'UR', icon: '⚡🚢', type: 'text', fallbackIcon: '⚡' }
    },
    crew: {
        'crew_none': { id: 'crew_none', name: 'ひとりぼっち', rarity: 'N', effect: 'none', skillName: 'がむしゃらアタック', desc: '乗組員はまだいない。\n【スペシャル】がむしゃらアタック（画面の敵を一掃！）', icon: '🧑‍🦲', type: 'text' },
        'crew_speed': { id: 'crew_speed', name: 'ダッシュマン・ジャック', rarity: 'R', effect: 'speedUp', skillName: '音速ハリケーン', desc: '船の動くスピードがアップするぞ！\n【スペシャル】音速ハリケーン（画面の敵を一掃！）', icon: '🏃', type: 'text' },
        'crew_score': { id: 'crew_score', name: '天才ナビゲーター', rarity: 'SR', effect: 'scoreUp', skillName: 'お宝ディスカバリー', desc: '敵を追い払った時のポイントが2倍に！\n【スペシャル】お宝ディスカバリー（画面の敵を一掃！）', icon: '🗺️', type: 'text' },
        'crew_power': { id: 'crew_power', name: '伝説のキャプテン', rarity: 'UR', effect: 'powerUp', skillName: '覇王の号令', desc: '弾のサイズと判定が巨大化\n【スペシャル】覇王の号令（画面の敵を一掃！）', icon: '👑', type: 'text' },
        'crew_seimaro': { id: 'crew_seimaro', name: 'せいまろ', rarity: 'UR', effect: 'doubleShot', skillName: '雷霆蒼破', desc: '大砲が2コになって、パワーが2倍だ！！\n【スペシャル】雷霆蒼破（いなずまで敵を一掃！）', icon: 'せいまろ.png', fallbackIcon: '🐾', type: 'image' }
    }
};

MASTER.stages = [];
for (let i = 1; i <= 100; i++) {
    let isBoss = i % 10 === 0; let cost = 5 + Math.floor((i - 1) / 10) * 5; 
    MASTER.stages.push({
        id: i, name: isBoss ? `海域 ${i} (BOSS級)` : `海域 ${i}`, goalScore: 20 + i * 5, enemySpeed: 1.5 + (i * 0.03),
        spawnRate: Math.max(15, 60 - Math.floor(i / 2)), reward: 1 + Math.floor(i / 10),
        color: isBoss ? 'border-red-500' : (i % 2 === 0 ? 'border-blue-400' : 'border-green-400'), isEndless: false, staminaCost: cost
    });
}

MASTER.exStages = [
    { id: 'ex_infinity', name: 'インフィニティステージ', goalScore: '∞', enemySpeed: 3, spawnRate: 30, reward: 'お見送り数次第', color: 'border-purple-500', isEndless: true, isEx: true, staminaCost: 20 },
    { id: 'ex_seimaro_1', name: 'せいまろの試練 1', goalScore: 50, enemySpeed: 2, spawnRate: 50, reward: 3, rewardCoins: 2000, color: 'border-pink-400', isEndless: false, isEx: true, reqClearEx: 0, staminaCost: 10 },
    { id: 'ex_seimaro_2', name: 'せいまろの試練 2', goalScore: 80, enemySpeed: 3, spawnRate: 35, reward: 5, rewardCoins: 3000, color: 'border-pink-500', isEndless: false, isEx: true, reqClearEx: 1, staminaCost: 20 },
    { id: 'ex_seimaro_3', name: 'せいまろの試練 3 (BOSS)', goalScore: 1, enemySpeed: 0.5, spawnRate: 99999, reward: 10, rewardCoins: 5000, rewardMats: 5, color: 'border-red-600', isEndless: false, isEx: true, isBossStage: true, reqClearEx: 2, staminaCost: 30 }
];

// ★ 新たに追加されたマルチステージ（Co-op と PvP）
MASTER.multiStages = [
    { id: 'multi_coop_1', name: '協力討伐！巨獣せいまろ', goalScore: 1, enemySpeed: 0.5, spawnRate: 99999, reward: 15, rewardCoins: 5000, rewardMats: 5, color: 'border-blue-500', isEndless: false, staminaCost: 20, isMulti: true, multiType: 'coop', isBossStage: true },
    { id: 'multi_pvp_1', name: '激突！フレンド対戦', goalScore: 1, enemySpeed: 2, spawnRate: 99999, reward: 10, rewardCoins: 3000, rewardMats: 3, color: 'border-red-500', isEndless: false, staminaCost: 15, isMulti: true, multiType: 'pvp', isBossStage: true }
];

MASTER.gachaPool = [
    { type: 'weapons', id: 'wp_spread' }, { type: 'weapons', id: 'wp_machine' }, { type: 'weapons', id: 'wp_legend' },
    { type: 'skins', id: 'skin_pirate' }, { type: 'skins', id: 'skin_ghost' },
    { type: 'skins', id: 'skin_wood' }, { type: 'skins', id: 'skin_iron' }, { type: 'skins', id: 'skin_benzai' }, { type: 'skins', id: 'skin_higaki' },
    { type: 'skins', id: 'skin_titanic' }, { type: 'skins', id: 'skin_yamato' }, { type: 'skins', id: 'skin_titus' },
    { type: 'skins', id: 'skin_kitamae' },
    { type: 'crew', id: 'crew_speed' }, { type: 'crew', id: 'crew_score' }, { type: 'crew', id: 'crew_power' }
];

MASTER.exGachaPool = [
    ...MASTER.gachaPool,
    { type: 'skins', id: 'skin_seimaro_ship' },
    { type: 'crew', id: 'crew_seimaro' }
];

const rarityColors = { 
    'N': 'text-slate-400', 'R': 'text-blue-400', 'SR': 'text-purple-400', 
    'SSR': 'text-transparent bg-clip-text bg-gradient-to-r from-orange-400 to-yellow-300 drop-shadow-sm',
    'UR': 'text-transparent bg-clip-text bg-gradient-to-r from-red-500 to-yellow-400 drop-shadow-[0_0_5px_rgba(255,200,0,0.8)]' 
};

let currentGachaTab = 'normal'; 
let happyShareTimeoutId = null; 
let currentStageTab = 'normal'; 

function saveGame() {
    try { localStorage.setItem('kaizoku', JSON.stringify(DB)); updateMyDataOnFirebase(); } catch (e) {}
}

function loadGame() {
    try {
        const saved = localStorage.getItem('kaizoku');
        if (saved) {
            DB = { ...DB, ...JSON.parse(saved) };
            if(DB.coins === undefined) DB.coins = 0; if(DB.materials === undefined) DB.materials = 0;
            if(!DB.itemLevels) DB.itemLevels = {}; if(!DB.lastShareTimestamp) DB.lastShareTimestamp = 0;
            if(DB.clearedSeimaroStage === undefined) DB.clearedSeimaroStage = 0;
            if(DB.stamina === undefined) { DB.stamina = 100; DB.lastStaminaUpdateTime = Date.now(); } 
        }
        if (!DB.userId || DB.userId === 'user_001') { DB.userId = 'PIRATE_' + Math.random().toString(36).substr(2, 9).toUpperCase(); saveGame(); }
    } catch (e) {}
}

function getItemLevel(itemId) { return DB.itemLevels[itemId] || 1; }
function getMaxStamina() { return 100 + (getItemLevel('stamina_cap') - 1) * 10; } 

setInterval(() => {
    if (!DB) return;
    const maxSt = getMaxStamina();
    if (DB.stamina < maxSt) {
        const now = Date.now(); const diff = now - DB.lastStaminaUpdateTime; const add = Math.floor(diff / 60000); 
        if (add > 0) {
            DB.stamina += add;
            if (DB.stamina > maxSt) { DB.stamina = maxSt; DB.lastStaminaUpdateTime = now; } 
            else { DB.lastStaminaUpdateTime += add * 60000; }
            saveGame();
            const stEl = document.getElementById('ui-stamina'); if (stEl) stEl.innerText = `${DB.stamina}/${maxSt}`;
        }
    } else { DB.lastStaminaUpdateTime = Date.now(); }
}, 1000);

function openStaminaModal() { document.getElementById('stamina-modal').classList.remove('hidden'); document.getElementById('stamina-modal').classList.add('flex'); }
function closeStaminaModal() { document.getElementById('stamina-modal').classList.add('hidden'); document.getElementById('stamina-modal').classList.remove('flex'); }
function buyStamina() {
    const maxSt = getMaxStamina();
    if (DB.stamina >= maxSt) { showAlert("スタミナはすでに満タンだ！\nこれ以上は回復できないぞ！"); return; }
    if (DB.coins < 500) { showAlert("草コインが足りないぞ！\n冒険して稼いでこよう！"); return; }
    DB.coins -= 500; DB.stamina += 50; 
    if (DB.stamina > maxSt) DB.stamina = maxSt;
    saveGame(); updateHeaderUI(); closeStaminaModal(); showAlert("草コインを使ってスタミナを回復したぞ！");
}

const firebaseConfig = { apiKey: "AIzaSyDSTydg6zf1Gf_PLW9crZ_QpQVQDtjcRu0", authDomain: "game-685f2.firebaseapp.com", databaseURL: "https://game-685f2-default-rtdb.firebaseio.com", projectId: "game-685f2", storageBucket: "game-685f2.firebasestorage.app", messagingSenderId: "266871071111", appId: "1:266871071111:web:ffff3b65d0234fc5abb322" };
let isFirebaseInitialized = false; let friendRequests = {}; let friendsList = {}; let currentFriendTab = 'list';

// ★ フレンド用の全データキャッシュ
let cachedFriendData = {};

function initFirebase() {
    try {
        firebase.initializeApp(firebaseConfig); window.db = firebase.database(); isFirebaseInitialized = true;
        updateMyDataOnFirebase();
        window.db.ref('friendRequests/' + DB.userId).on('value', snap => { friendRequests = snap.val() || {}; updateFriendReqBadge(); if (currentFriendTab === 'req') renderFriendList(); });
        window.db.ref('friends/' + DB.userId).on('value', snap => { friendsList = snap.val() || {}; if (currentFriendTab === 'list') renderFriendList(); });
        let isInitialShareLoad = true;
        window.db.ref('global/latest_ur_pull').on('value', snap => {
            const data = snap.val();
            if (data && !isInitialShareLoad) {
                if (data.timestamp > DB.lastShareTimestamp && data.userId !== DB.userId) { DB.lastShareTimestamp = data.timestamp; saveGame(); triggerHappyShare(data.userName, data.itemName); }
            }
            isInitialShareLoad = false;
        });
    } catch(e) { console.warn("Firebase Init Failed", e); }
}

function updateMyDataOnFirebase() { 
    if (!isFirebaseInitialized) return; 
    window.db.ref('users/' + DB.userId).set({ 
        userName: DB.userName, 
        userIcon: DB.userIcon, 
        clearedStages: DB.clearedStages,
        equippedWeapon: DB.equippedWeapon,
        equippedSkin: DB.equippedSkin,
        equippedCrew: DB.equippedCrew,
        itemLevels: DB.itemLevels || {}
    }); 
}

function copyMyId() { const tempInput = document.createElement("input"); tempInput.value = DB.userId; document.body.appendChild(tempInput); tempInput.select(); try { document.execCommand("copy"); showAlert("キミの海賊IDをコピーしたぞ！\n友達に教えよう！"); } catch(err) { showAlert("コピーに失敗したぜ...。\n手動でメモしてくれ！"); } document.body.removeChild(tempInput); }
function switchFriendTab(tab) {
    currentFriendTab = tab; const btnList = document.getElementById('tab-friend-list'); const btnReq = document.getElementById('tab-friend-req');
    if (tab === 'list') { btnList.className = "flex-1 py-3 bg-indigo-600 text-white font-black rounded-lg border-b-4 border-indigo-800 text-sm transition-all shadow-md"; btnReq.className = "flex-1 py-3 bg-indigo-950 text-indigo-400 font-black rounded-lg border-b-4 border-transparent text-sm transition-all relative shadow-md"; } 
    else { btnList.className = "flex-1 py-3 bg-indigo-950 text-indigo-400 font-black rounded-lg border-b-4 border-transparent text-sm transition-all shadow-md"; btnReq.className = "flex-1 py-3 bg-indigo-600 text-white font-black rounded-lg border-b-4 border-indigo-800 text-sm transition-all relative shadow-md"; }
    renderFriendList();
}
function updateFriendReqBadge() { const count = Object.keys(friendRequests).length; const badge = document.getElementById('friend-req-badge'); if (count > 0) { badge.innerText = "!"; badge.classList.remove('hidden'); } else { badge.classList.add('hidden'); } }

async function renderFriendList() {
    const container = document.getElementById('friend-list-container'); container.innerHTML = '<div class="text-center text-indigo-300 font-bold mt-4 animate-pulse">読み込み中...</div>';
    if (!isFirebaseInitialized) { container.innerHTML = '<div class="text-center text-red-400 font-bold mt-4">通信エラーが発生しているぞ！</div>'; return; }
    const targetObj = currentFriendTab === 'list' ? friendsList : friendRequests; const userIds = Object.keys(targetObj);
    if (userIds.length === 0) {
        container.innerHTML = `<div class="text-center text-indigo-300 font-bold mt-4 bg-indigo-900/50 p-4 rounded-xl border border-indigo-800">${currentFriendTab === 'list' ? 'まだ海賊団の仲間がいないぞ。<br>上の検索から友達のIDを入力して申請しよう！' : '届いている申請はないぞ。'}</div>`; return;
    }
    let html = '';
    for (let uid of userIds) {
        try {
            const snap = await window.db.ref('users/' + uid).once('value'); const userData = snap.val(); if (!userData) continue;
            
            // 仲間データをキャッシュ
            cachedFriendData[uid] = userData;

            let iconHtml = '';
            if (userData.userIcon && userData.userIcon.startsWith('data:image')) { iconHtml = `<img src="${userData.userIcon}" class="w-12 h-12 rounded-xl object-cover border-2 border-indigo-400 shadow-md">`; } 
            else { iconHtml = `<div class="w-12 h-12 bg-indigo-800 rounded-xl flex items-center justify-center text-2xl border-2 border-indigo-400 shadow-md">${userData.userIcon || '👤'}</div>`; }
            let actionHtml = '';
            if (currentFriendTab === 'req') { actionHtml = `<button onclick="acceptFriend('${uid}')" class="game-btn bg-gradient-to-b from-green-400 to-green-600 text-white text-xs font-black px-4 py-2 rounded-lg border-2 border-white shadow-md">承認</button><button onclick="rejectFriend('${uid}')" class="game-btn bg-gradient-to-b from-red-400 to-red-600 text-white text-xs font-black px-3 py-2 rounded-lg border-2 border-white shadow-md">✕</button>`; } 
            else { actionHtml = `<div class="text-[10px] text-yellow-300 font-black bg-indigo-950 px-2 py-1 rounded border border-indigo-700">海域 ${userData.clearedStages || 0}</div>`; }
            html += `<div class="bg-indigo-900/80 border-2 border-indigo-700 p-3 rounded-2xl flex items-center gap-3 shadow-lg pop-in">${iconHtml}<div class="flex-1 overflow-hidden"><div class="font-black text-white text-base truncate drop-shadow-md">${userData.userName}</div><div class="text-[9px] text-indigo-300 font-mono tracking-wider">ID: ${uid}</div></div><div class="flex items-center gap-1">${actionHtml}</div></div>`;
        } catch(e) {}
    }
    container.innerHTML = html;
}
function sendFriendRequest() {
    if (!isFirebaseInitialized) return showAlert("通信エラーだ！今は申請できないぞ！");
    const input = document.getElementById('friend-search-input'); const targetId = input.value.trim().toUpperCase();
    if (!targetId) return showAlert("友達の海賊IDを入力してくれ！"); if (targetId === DB.userId) return showAlert("それは自分のIDだぞ！");
    window.db.ref('users/' + targetId).once('value').then(snap => { if (!snap.exists()) { showAlert("そのIDの海賊は見つからなかったぞ！"); return; } window.db.ref('friendRequests/' + targetId + '/' + DB.userId).set(Date.now()).then(() => { showAlert("フレンド申請を送ったぞ！"); input.value = ''; }); });
}
function acceptFriend(uid) { if (!isFirebaseInitialized) return; window.db.ref('friends/' + DB.userId + '/' + uid).set(true); window.db.ref('friends/' + uid + '/' + DB.userId).set(true); window.db.ref('friendRequests/' + DB.userId + '/' + uid).remove(); }
function rejectFriend(uid) { if (!isFirebaseInitialized) return; window.db.ref('friendRequests/' + DB.userId + '/' + uid).remove(); }

/**
 * 📅 ログインボーナスシステム
 */
function checkLoginBonus() {
    const todayStr = new Date().toDateString(); 
    if (DB.lastLoginDate !== todayStr) { let streak = DB.loginStreak || 0; if (streak >= 7) streak = 0; DB.loginStreak = streak + 1; DB.lastLoginDate = todayStr; saveGame(); showLoginBonusPopup(); }
}
function showLoginBonusPopup() {
    const streak = DB.loginStreak; const grid = document.getElementById('login-grid'); grid.innerHTML = ''; const todayReward = LOGIN_BONUS_MASTER[streak - 1];
    LOGIN_BONUS_MASTER.forEach(b => {
        const isClaimed = b.day < streak; const isToday = b.day === streak;
        const cell = document.createElement('div');
        cell.className = `p-2 rounded-xl flex flex-col items-center justify-center relative border-2 ${isToday ? 'bg-indigo-600 border-yellow-400 shadow-lg scale-105 z-10' : isClaimed ? 'bg-indigo-950/60 border-indigo-900 opacity-60' : 'bg-indigo-950 border-indigo-800'}`;
        let rewardVisual = b.type === 'special' ? `<div class="text-xl">⭐</div>` : `<span class="inline-flex relative w-6 h-6"><img src="${b.icon}" class="absolute inset-0 w-full h-full object-contain bg-transparent z-10" onerror="fb(this,'${b.fallback}','text-lg')"></span>`;
        cell.innerHTML = `<div class="text-[9px] font-black text-indigo-300">${b.day}日目</div><div class="my-1.5 h-7 flex items-center justify-center">${rewardVisual}</div><div class="text-[10px] text-white font-black">${b.type === 'special' ? '大入' : '×' + b.amount}</div>${isClaimed ? '<div class="absolute inset-0 bg-black/45 rounded-xl flex items-center justify-center font-black text-xs text-green-400">✔️ 済</div>' : ''}${isToday ? '<div class="absolute -top-1 bg-yellow-500 text-indigo-950 font-black text-[8px] px-1 rounded shadow-md animate-pulse">本日!</div>' : ''}`;
        grid.appendChild(cell);
    });
    const todayVisual = document.getElementById('login-today-reward-visual');
    if (todayReward.type === 'special') {
        todayVisual.innerHTML = `<span class="inline-flex relative w-7 h-7"><img src="草核.png" class="absolute inset-0 w-full h-full object-contain z-10" onerror="fb(this,'✨','text-xl')"></span><span class="text-yellow-400 text-lg font-black">草核×${todayReward.amount}</span><span class="text-indigo-400 font-bold mx-1">+</span><span class="inline-flex relative w-7 h-7"><img src="草コイン.png" class="absolute inset-0 w-full h-full object-contain z-10" onerror="fb(this,'💰','text-xl')"></span><span class="text-yellow-400 text-lg font-black">×${todayReward.coinsBonus}</span>`;
    } else {
        todayVisual.innerHTML = `<span class="inline-flex relative w-8 h-8 mr-2"><img src="${todayReward.icon}" class="absolute inset-0 w-full h-full object-contain z-10" onerror="fb(this,'${todayReward.fallback}','text-2xl')"></span><span class="text-yellow-400 text-2xl font-black">${todayReward.label} ×${todayReward.amount}</span>`;
    }
    document.getElementById('login-bonus-modal').classList.remove('hidden'); document.getElementById('login-bonus-modal').classList.add('flex'); shootConfetti('login-confetti-container', 80);
}
function claimLoginBonus() {
    const streak = DB.loginStreak; const reward = LOGIN_BONUS_MASTER[streak - 1];
    if (reward.type === 'coins') DB.coins += reward.amount; else if (reward.type === 'materials') DB.materials += reward.amount; else if (reward.type === 'tickets') DB.gachaTickets += reward.amount; else if (reward.type === 'special') { DB.gachaTickets += reward.amount; DB.coins += reward.coinsBonus; }
    saveGame(); updateHeaderUI(); document.getElementById('login-bonus-modal').classList.add('hidden'); document.getElementById('login-bonus-modal').classList.remove('flex'); document.getElementById('login-confetti-container').innerHTML = '';
}

function openGachaProbabilityModal() { document.getElementById('gacha-probability-modal').classList.remove('hidden'); document.getElementById('gacha-probability-modal').classList.add('flex'); }
function closeGachaProbabilityModal() { document.getElementById('gacha-probability-modal').classList.add('hidden'); document.getElementById('gacha-probability-modal').classList.remove('flex'); }

/**
 * 🦜 チュートリアル制御
 */
const TUTORIAL_SCENARIOS = {
    'battle': [ { character: '🦜', text: 'ようこそ！「ルージュ・ガレオン」の世界へ！！\nオレ様はナビゲーターのナビーだ！\n大海原にはライバル海賊がいっぱいいるぞ。', targetId: null }, { character: '🦜', text: 'まずは船のそうじゅうに慣れるために、\n「大冒険へ出発!!」を押して初航海に出よう！', targetId: 'btn-start-navigation' } ],
    'gacha': [ { character: '🦜', text: '初航海おつかれさま！見事なそうじゅうだったぜ！\n手に入れた「草核」を使って宝箱をあけよう！\n新しい大砲や乗組員が手に入るかも！？', targetId: 'btn-draw-gacha-1' } ],
    'gacha_done': [ { character: '🦜', text: 'すっげーお宝を手に入れたな！！\nさっそく下の「準備」ボタンを押して、\n今のお宝を船にセットしに行こうぜ！', targetId: 'nav-btn-equip' } ],
    'equip': [ { character: '🦜', text: 'ここが準備の画面だ！\n手に入れたお宝のボタンをタップして、\n船をパワーアップさせてみよう！！', targetId: 'tutorial-target-equip' } ],
    'all_done': [ { character: '🦜', text: 'カンペキだ、船長！！\nこれで冒険の準備はすべて完了したぜ！\nさあ、伝説の財宝をさがす大冒険の始まりだ！！', targetId: null } ]
};

let currentTutorialScenario = null; let tutorialStep = 0;
function startSpecificTutorial(scenarioName) { currentTutorialScenario = scenarioName; tutorialStep = 0; showTutorialStep(); }
function checkAndStartTutorial() {
    updateLockStatus();
    if (!DB.tutorialCompleted) startSpecificTutorial('battle');
    else if (!DB.tutorialGachaCompleted) { if (!document.getElementById('screen-gacha').classList.contains('hidden')) startSpecificTutorial('gacha'); else positionTutorialArrow('nav-btn-gacha'); }
    else if (!DB.tutorialEquipCompleted) { if (!document.getElementById('screen-equip').classList.contains('hidden')) startSpecificTutorial('equip'); else positionTutorialArrow('nav-btn-equip'); }
    else positionTutorialArrow(null);
}
function positionTutorialArrow(targetId) {
    const arrow = document.getElementById('tutorial-arrow');
    if (!targetId) { arrow.classList.add('hidden'); return; }
    const target = document.getElementById(targetId); if (!target) { arrow.classList.add('hidden'); return; }
    const appContainer = document.getElementById('app'); const appRect = appContainer.getBoundingClientRect(); const targetRect = target.getBoundingClientRect();
    arrow.style.top = `${targetRect.top - appRect.top - 60}px`; arrow.style.left = `${targetRect.left - appRect.left + (targetRect.width / 2) - 24}px`; arrow.classList.remove('hidden');
}
function showTutorialStep() {
    const overlay = document.getElementById('tutorial-overlay'); const steps = TUTORIAL_SCENARIOS[currentTutorialScenario];
    if (!steps || !steps[tutorialStep]) { overlay.classList.add('hidden'); positionTutorialArrow(null); return; }
    const stepData = steps[tutorialStep]; overlay.classList.remove('hidden'); overlay.classList.add('flex');
    const counter = document.getElementById('tutorial-step-counter');
    if (steps.length > 1) { counter.innerText = `${tutorialStep + 1}/${steps.length}`; counter.classList.remove('hidden'); } else counter.classList.add('hidden');
    document.getElementById('tutorial-character').innerText = stepData.character; document.getElementById('tutorial-text').innerText = stepData.text;
    document.querySelectorAll('.tutorial-highlight').forEach(el => el.classList.remove('tutorial-highlight'));
    if (stepData.targetId) { const target = document.getElementById(stepData.targetId); if (target) { target.classList.add('tutorial-highlight'); setTimeout(() => { positionTutorialArrow(stepData.targetId); }, 100); } } else positionTutorialArrow(null);
}
function nextTutorialStep() {
    const steps = TUTORIAL_SCENARIOS[currentTutorialScenario]; tutorialStep++;
    if (tutorialStep >= steps.length) {
        document.getElementById('tutorial-overlay').classList.add('hidden'); document.getElementById('tutorial-overlay').classList.remove('flex'); document.querySelectorAll('.tutorial-highlight').forEach(el => el.classList.remove('tutorial-highlight'));
        const lastTarget = steps[steps.length - 1].targetId;
        if (lastTarget) { const btn = document.getElementById(lastTarget); if (btn) btn.classList.add('animate-pulse'); positionTutorialArrow(lastTarget); } else positionTutorialArrow(null);
        if (currentTutorialScenario === 'all_done') { document.querySelectorAll('.animate-pulse').forEach(el => el.classList.remove('animate-pulse')); document.getElementById('special-ui-container').classList.remove('hidden'); }
    } else showTutorialStep();
}

function tryAccessLockedScreen(screenId) {
    if (!DB.tutorialCompleted) { document.getElementById('locked-alert-overlay').classList.remove('hidden'); document.getElementById('locked-alert-overlay').classList.add('flex'); return; }
    navTo(screenId);
}
function closeLockedAlert() { document.getElementById('locked-alert-overlay').classList.add('hidden'); document.getElementById('locked-alert-overlay').classList.remove('flex'); }

function updateLockStatus() {
    const lockEquip = document.getElementById('nav-equip-lock'); const lockGacha = document.getElementById('nav-gacha-lock'); const lockFriend = document.getElementById('nav-friend-lock');
    const btnEquip = document.getElementById('nav-btn-equip'); const btnGacha = document.getElementById('nav-btn-gacha'); const btnFriend = document.getElementById('nav-btn-friend');
    if (DB.tutorialCompleted) {
        if(lockEquip) lockEquip.classList.add('hidden'); if(lockGacha) lockGacha.classList.add('hidden'); if(lockFriend) lockFriend.classList.add('hidden');
        if(btnEquip) btnEquip.classList.remove('opacity-50'); if(btnGacha) btnGacha.classList.remove('opacity-50'); if(btnFriend) btnFriend.classList.remove('opacity-50');
    } else {
        if(lockEquip) lockEquip.classList.remove('hidden'); if(lockGacha) lockGacha.classList.remove('hidden'); if(lockFriend) lockFriend.classList.remove('hidden');
        if(btnEquip) btnEquip.classList.add('opacity-50'); if(btnGacha) btnGacha.classList.add('opacity-50'); if(btnFriend) btnFriend.classList.add('opacity-50');
    }
}

function handleTutorialClear() {
    DB.tutorialCompleted = true; DB.gachaTickets += 10; DB.coins += 2000;
    positionTutorialArrow(null); updateLockStatus(); updateHeaderUI(); saveGame(); 
    const btn = document.getElementById('btn-start-navigation'); if (btn) btn.classList.remove('animate-pulse');
    document.getElementById('tutorial-clear-modal').classList.remove('hidden'); document.getElementById('tutorial-clear-modal').classList.add('flex'); shootConfetti('tutorial-confetti-container', 100);
}
function finishTutorialClear() {
    document.getElementById('tutorial-clear-modal').classList.add('hidden'); document.getElementById('tutorial-clear-modal').classList.remove('flex'); document.getElementById('tutorial-confetti-container').innerHTML = ''; 
    navTo('screen-home'); setTimeout(() => { positionTutorialArrow('nav-btn-gacha'); }, 400);
}

function showLogoFallback(imgEl) { imgEl.style.display = 'none'; document.getElementById('title-fallback').classList.remove('hidden'); }

let tempSelectedIcon = ''; let customIconDataUrl = ''; 
function handleIconUpload(event) {
    const file = event.target.files[0];
    if (file) {
        const reader = new FileReader();
        reader.onload = function(e) {
            const img = new Image();
            img.onload = function() {
                const canvas = document.createElement('canvas'); canvas.width = 100; canvas.height = 100; const ctx = canvas.getContext('2d');
                const size = Math.min(img.width, img.height); const sx = (img.width - size) / 2; const sy = (img.height - size) / 2;
                ctx.fillStyle = "#ffffff"; ctx.fillRect(0, 0, 100, 100); ctx.drawImage(img, sx, sy, size, size, 0, 0, 100, 100);
                customIconDataUrl = canvas.toDataURL('image/jpeg', 0.8); document.getElementById('custom-icon-img').src = customIconDataUrl; document.getElementById('custom-icon-preview-container').classList.remove('hidden'); selectIcon('CUSTOM_IMAGE');
            }; img.src = e.target.result;
        }; reader.readAsDataURL(file);
    }
}
function openProfileModal() {
    tempSelectedIcon = DB.userIcon; document.getElementById('edit-username').value = DB.userName;
    if (tempSelectedIcon && tempSelectedIcon.startsWith('data:image')) { customIconDataUrl = tempSelectedIcon; document.getElementById('custom-icon-img').src = customIconDataUrl; document.getElementById('custom-icon-preview-container').classList.remove('hidden'); }
    updateIconSelectionUI(); document.getElementById('profile-modal').classList.remove('hidden'); document.getElementById('profile-modal').classList.add('flex');
}
function selectIcon(iconValue) { if (iconValue === 'CUSTOM_IMAGE') { tempSelectedIcon = customIconDataUrl; } else { tempSelectedIcon = iconValue; } updateIconSelectionUI(); }
function updateIconSelectionUI() {
    document.querySelectorAll('.icon-sel-btn').forEach(btn => {
        if (btn.id === 'custom-icon-btn') { if (tempSelectedIcon && tempSelectedIcon.startsWith('data:image') && tempSelectedIcon === customIconDataUrl) { btn.classList.add('border-yellow-400', 'shadow-[0_0_10px_yellow]'); btn.classList.remove('border-indigo-800'); } else { btn.classList.remove('border-yellow-400', 'shadow-[0_0_10px_yellow]'); btn.classList.add('border-indigo-800'); } } 
        else { if(btn.innerText === tempSelectedIcon) { btn.classList.add('bg-indigo-700', 'border-blue-400'); btn.classList.remove('border-transparent'); } else { btn.classList.remove('bg-indigo-700', 'border-blue-400'); btn.classList.add('border-transparent'); } }
    });
}
function saveProfile() { const newName = document.getElementById('edit-username').value.trim(); if (newName) DB.userName = newName; DB.userIcon = tempSelectedIcon; updateHeaderUI(); closeProfileModal(); saveGame(); }
function closeProfileModal() { document.getElementById('profile-modal').classList.add('hidden'); document.getElementById('profile-modal').classList.remove('flex'); }

function updateHeaderUI() {
    document.getElementById('ui-username').innerText = DB.userName;
    const iconContainer = document.getElementById('ui-usericon');
    if (DB.userIcon && DB.userIcon.startsWith('data:image')) iconContainer.innerHTML = `<img src="${DB.userIcon}" class="w-full h-full object-cover">`; else iconContainer.innerText = DB.userIcon;
    document.getElementById('ui-tickets').innerText = DB.gachaTickets; document.getElementById('ui-coins').innerText = DB.coins; document.getElementById('ui-materials').innerText = DB.materials;
    const maxSt = getMaxStamina(); document.getElementById('ui-stamina').innerText = `${DB.stamina}/${maxSt}`;
    document.getElementById('my-friend-id').innerText = DB.userId;
}

function navTo(screenId) {
    positionTutorialArrow(null); document.querySelectorAll('.animate-pulse').forEach(el => el.classList.remove('animate-pulse')); document.querySelectorAll('.screen').forEach(el => el.classList.add('hidden'));
    const targetScreen = document.getElementById(screenId); targetScreen.classList.remove('hidden'); targetScreen.style.animation = 'none'; targetScreen.offsetHeight; targetScreen.style.animation = null;
    document.querySelectorAll('.nav-btn').forEach(btn => { if (btn.dataset.target === screenId) { btn.classList.add('text-yellow-400', 'scale-110'); btn.classList.remove('text-indigo-400'); } else { btn.classList.remove('text-yellow-400', 'scale-110'); btn.classList.add('text-indigo-400'); } });
    if (screenId === 'screen-home') { initHome(); if (isAudioInitialized) homeBgm.play().catch(()=>{}); } else { homeBgm.pause(); }
    
    if (screenId === 'screen-stage') initStageSelect(); if (screenId === 'screen-equip') initEquip(); if (screenId === 'screen-gacha') updateGachaUI(); if (screenId === 'screen-friend') { if (currentFriendTab === 'list') renderFriendList(); }
    if (screenId !== 'screen-game') { stopGame(); document.getElementById('app-header').classList.remove('hidden'); document.getElementById('app-nav').classList.remove('hidden'); document.getElementById('bubbles-layer').style.display = 'block'; } 
    else { document.getElementById('app-header').classList.add('hidden'); document.getElementById('app-nav').classList.add('hidden'); document.getElementById('bubbles-layer').style.display = 'none'; }
    if (screenId === 'screen-gacha' && DB.tutorialCompleted && !DB.tutorialGachaCompleted) setTimeout(() => startSpecificTutorial('gacha'), 500); else if (screenId === 'screen-equip' && DB.tutorialGachaCompleted && !DB.tutorialEquipCompleted) setTimeout(() => startSpecificTutorial('equip'), 500); else if (screenId === 'screen-home') checkAndStartTutorial();
}

function initHome() {
    updateHeaderUI();
    const skin = MASTER.skins[DB.equippedSkin]; const weapon = MASTER.weapons[DB.equippedWeapon]; const crew = MASTER.crew[DB.equippedCrew];
    document.getElementById('home-skin-icon').innerText = skin.icon; document.getElementById('home-skin').innerText = skin.name; document.getElementById('home-weapon-icon').innerText = '💣'; document.getElementById('home-weapon').innerText = weapon.name;
    const crewContainer = document.getElementById('home-crew-container'); crewContainer.innerHTML = '';
    if (crew.type === 'image') {
        crewContainer.innerHTML = `<span class="inline-flex relative w-10 h-10 filter drop-shadow-md"><img src="${crew.icon}" class="absolute inset-0 w-full h-full object-contain" onerror="fb(this,'${crew.fallbackIcon}','text-3xl filter drop-shadow-md')"></span>`;
    } else {
        crewContainer.innerHTML = `<span class="text-4xl filter drop-shadow-md">${crew.icon}</span>`;
    }
    document.getElementById('home-crew').innerText = crew.name;
}

window._tutorialEquipTargetSet = false; let currentDetailItem = null; let currentDetailKey = null;
function initEquip() { window._tutorialEquipTargetSet = false; updateHeaderUI(); renderEquipList('equip-weapons', MASTER.weapons, DB.ownedWeapons, DB.equippedWeapon, 'equippedWeapon'); renderEquipList('equip-skins', MASTER.skins, DB.ownedSkins, DB.equippedSkin, 'equippedSkin'); renderEquipList('equip-crew', MASTER.crew, DB.ownedCrew, DB.equippedCrew, 'equippedCrew'); renderEquipList('equip-system', MASTER.systems, ['stamina_cap'], null, 'system'); }
function renderEquipList(containerId, masterData, ownedList, equippedId, dbKey) {
    const container = document.getElementById(containerId); container.innerHTML = '';
    ownedList.forEach(id => {
        const item = masterData[id]; const isEquipped = id === equippedId; const level = getItemLevel(id); const btn = document.createElement('button');
        btn.className = `p-2 rounded-xl border-2 text-left relative overflow-hidden transition-transform transform hover:scale-105 active:scale-95 ${isEquipped ? 'bg-indigo-600 border-yellow-400 shadow-[0_0_15px_rgba(250,204,21,0.5)]' : 'bg-indigo-950 border-indigo-700 shadow-md'}`;
        let visualHTML = item.type === 'image' ? `<div class="relative w-8 h-8 flex items-center justify-center bg-indigo-900 rounded-lg overflow-hidden border border-indigo-700"><img src="${item.icon}" class="absolute inset-0 w-full h-full object-contain" onerror="fb(this,'${item.fallbackIcon}','text-sm')"></div>` : `<span class="text-2xl filter drop-shadow-md">${item.icon || '⚔️'}</span>`;
        if (dbKey === 'system') { btn.innerHTML = `<div class="flex justify-between items-start mb-1"><div class="text-yellow-400 font-black text-[10px] drop-shadow-sm">[システム]</div><div class="text-yellow-300 text-[10px] font-black bg-indigo-900/80 px-1 rounded-sm border border-indigo-700">Lv.${level}</div></div><div class="flex items-center gap-2">${visualHTML}<div class="font-bold text-white text-xs leading-tight">${item.name}<br><span class="text-[9px] text-indigo-300">最大値UP!</span></div></div>`; } 
        else { btn.innerHTML = `<div class="flex justify-between items-start mb-1"><div class="${rarityColors[item.rarity]} font-black text-[10px] drop-shadow-sm">[${item.rarity}]</div><div class="text-yellow-300 text-[10px] font-black bg-indigo-900/80 px-1 rounded-sm border border-indigo-700">Lv.${level}</div></div><div class="flex items-center gap-2">${visualHTML}<div class="font-bold text-white text-xs leading-tight">${item.name}</div></div>${isEquipped ? '<div class="absolute top-0 right-0 bg-yellow-500 text-indigo-950 text-[10px] px-2 py-0.5 rounded-bl-lg font-black shadow-sm border-b border-l border-yellow-200">装備中!!</div>' : ''}`; }
        if (!isEquipped && dbKey !== 'system' && !window._tutorialEquipTargetSet && !DB.tutorialEquipCompleted && currentTutorialScenario === 'equip') { btn.id = 'tutorial-target-equip'; window._tutorialEquipTargetSet = true; }
        btn.onclick = () => { openItemDetail(id, dbKey); }; container.appendChild(btn);
    });
}
function openItemDetail(itemId, dbKey) {
    let masterType = dbKey === 'equippedWeapon' ? 'weapons' : dbKey === 'equippedSkin' ? 'skins' : dbKey === 'equippedCrew' ? 'crew' : 'systems';
    const mItem = MASTER[masterType][itemId]; currentDetailItem = mItem; currentDetailKey = dbKey; const level = getItemLevel(mItem.id);
    document.getElementById('detail-rarity').innerText = mItem.rarity || 'System'; document.getElementById('detail-rarity').className = `font-black text-lg ${mItem.rarity ? rarityColors[mItem.rarity] : 'text-yellow-400'}`; document.getElementById('detail-level').innerText = `Lv.${level}`;
    const iconCont = document.getElementById('detail-icon'); iconCont.innerHTML = '';
    if (mItem.type === 'image') iconCont.innerHTML = `<span class="inline-flex relative w-16 h-16 filter drop-shadow-md"><img src="${mItem.icon}" class="absolute inset-0 w-full h-full object-contain" onerror="fb(this,'${mItem.fallbackIcon}','text-5xl')"></span>`;
    else iconCont.innerHTML = `<span class="text-6xl filter drop-shadow-md">${mItem.icon}</span>`;
    document.getElementById('detail-name').innerText = mItem.name; document.getElementById('detail-desc').innerText = mItem.desc;
    const reqCoins = level * 200; const reqMats = Math.floor(level / 2) + 1;
    document.getElementById('req-coins').innerText = reqCoins; document.getElementById('req-materials').innerText = reqMats;
    const btnUpgrade = document.getElementById('btn-upgrade');
    if (level >= 10) { btnUpgrade.innerHTML = "MAX レベル!! 🌟"; btnUpgrade.disabled = true; btnUpgrade.classList.add('opacity-50', 'cursor-not-allowed', 'from-gray-500', 'to-gray-700'); btnUpgrade.classList.remove('from-green-400', 'to-green-600', 'active:scale-95'); } 
    else { btnUpgrade.innerHTML = "パワーアップ!! ✨"; btnUpgrade.disabled = false; btnUpgrade.classList.remove('opacity-50', 'cursor-not-allowed', 'from-gray-500', 'to-gray-700'); btnUpgrade.classList.add('from-green-400', 'to-green-600', 'active:scale-95'); }
    if (dbKey === 'system') document.getElementById('btn-equip').classList.add('hidden'); else document.getElementById('btn-equip').classList.remove('hidden');
    document.getElementById('item-detail-modal').classList.remove('hidden'); document.getElementById('item-detail-modal').classList.add('flex');
}
function closeItemDetail() { document.getElementById('item-detail-modal').classList.add('hidden'); document.getElementById('item-detail-modal').classList.remove('flex'); }
function equipItem() {
    if (!currentDetailItem || !currentDetailKey || currentDetailKey === 'system') return; DB[currentDetailKey] = currentDetailItem.id; saveGame(); initEquip(); closeItemDetail();
    if (!DB.tutorialEquipCompleted && currentTutorialScenario === 'equip') { DB.tutorialEquipCompleted = true; saveGame(); positionTutorialArrow(null); setTimeout(() => startSpecificTutorial('all_done'), 300); }
}
function upgradeItem() {
    if (!currentDetailItem) return; const level = getItemLevel(currentDetailItem.id); if (level >= 10) return;
    const reqCoins = level * 200; const reqMats = Math.floor(level / 2) + 1;
    if (DB.coins < reqCoins || DB.materials < reqMats) { showAlert("草コインか草の化石が足りないぞ！\n冒険したり、ガチャのダブりで集めよう！"); return; }
    DB.coins -= reqCoins; DB.materials -= reqMats; DB.itemLevels[currentDetailItem.id] = level + 1; saveGame(); updateHeaderUI(); openItemDetail(currentDetailItem.id, currentDetailKey); initEquip(); 
}

function switchGachaTab(tabType) {
    currentGachaTab = tabType; const btnNormal = document.getElementById('tab-gacha-normal'); const btnEx = document.getElementById('tab-gacha-ex'); const titleEl = document.getElementById('gacha-title'); const subEl = document.getElementById('gacha-sub'); const emojiEl = document.getElementById('gacha-chest-emoji'); const badgeEl = document.getElementById('ex-pickup-badge');
    if (tabType === 'normal') {
        btnNormal.className = "flex-1 py-3 rounded-lg font-black text-sm bg-indigo-600 text-white shadow-md transition-all border-b-4 border-indigo-800"; btnEx.className = "flex-1 py-3 rounded-lg font-black text-sm text-indigo-400 hover:text-white transition-all relative border-b-4 border-transparent";
        titleEl.innerText = "わくわく通常ガチャ"; titleEl.className = "text-3xl font-black text-transparent bg-clip-text bg-gradient-to-b from-cyan-200 to-blue-500 drop-shadow-md"; subEl.innerText = "いろんなお宝が眠っているぞ！"; emojiEl.innerText = "🏴‍☠️"; badgeEl.classList.add('hidden');
    } else {
        btnNormal.className = "flex-1 py-3 rounded-lg font-black text-sm text-slate-400 hover:text-white transition-all border-b-4 border-transparent"; btnEx.className = "flex-1 py-3 rounded-lg font-black text-sm bg-indigo-600 text-white shadow-md transition-all relative border-b-4 border-indigo-800";
        titleEl.innerText = "⭐ 超・限定EXガチャ"; titleEl.className = "text-3xl font-black text-transparent bg-clip-text bg-gradient-to-b from-pink-300 to-yellow-400 drop-shadow-md"; subEl.innerText = "今だけ！すごい乗組員がゲットできるかも！"; emojiEl.innerText = "✨🎁✨"; badgeEl.classList.remove('hidden');
    }
}
function updateGachaUI() { updateHeaderUI(); let urRate = Math.min(1.0 + (DB.pityCounter * 0.5), 100.0).toFixed(1); document.getElementById('gacha-rate').innerText = `${urRate}%`; let progress = Math.min((DB.pityCounter / 50) * 100, 100); document.getElementById('pity-bar').style.width = `${progress}%`; }

let currentGachaResults = [];
function startGachaAnim(times) {
    if (!document.getElementById('tutorial-overlay').classList.contains('hidden')) return;
    
    // チュートリアルガチャの時はチケット消費を免除（詰み防止）
    if (DB.gachaTickets < times && DB.tutorialGachaCompleted) { showAlert("草核が足りないぞ！冒険して集めよう！"); return; }
    if (DB.gachaTickets >= times) { DB.gachaTickets -= times; }

    currentGachaResults = []; let hasUR = false; let highestRarityItem = null;
    for (let i = 0; i < times; i++) {
        let urRate = 0.01 + (DB.pityCounter * 0.005); let ssrRate = 0.04; let srRate = 0.10; let rRate = 0.30; let rand = Math.random(); let resultRarity = 'N';
        if (rand < urRate) resultRarity = 'UR'; else if (rand < urRate + ssrRate) resultRarity = 'SSR'; else if (rand < urRate + ssrRate + srRate) resultRarity = 'SR'; else if (rand < urRate + ssrRate + srRate + rRate) resultRarity = 'R';
        const activePool = currentGachaTab === 'normal' ? MASTER.gachaPool : MASTER.exGachaPool;
        let pool = activePool.filter(i => MASTER[i.type][i.id].rarity === resultRarity); if (pool.length === 0) { resultRarity = 'R'; pool = activePool.filter(i => MASTER[i.type][i.id].rarity === 'R'); }
        let drawnItem = pool[Math.floor(Math.random() * pool.length)]; let masterItem = MASTER[drawnItem.type][drawnItem.id];
        let ownedKey = drawnItem.type === 'weapons' ? 'ownedWeapons' : drawnItem.type === 'skins' ? 'ownedSkins' : 'ownedCrew';
        let isNew = !DB[ownedKey].includes(drawnItem.id);
        if (isNew) { DB[ownedKey].push(drawnItem.id); } else { DB.coins += 500; DB.materials += 1; }
        if (resultRarity === 'UR') { hasUR = true; highestRarityItem = masterItem; DB.pityCounter = 0; } else { DB.pityCounter++; }
        currentGachaResults.push({ item: masterItem, isNew: isNew });
    }
    saveGame(); positionTutorialArrow(null); 
    const animCont = document.getElementById('gacha-anim-container'); const chest = document.getElementById('gacha-chest'); const urLayer = document.getElementById('ur-anim-layer'); const flashText = document.getElementById('ur-flash-text');
    animCont.classList.remove('hidden'); animCont.classList.add('flex');
    chest.innerText = times === 10 ? '📦📦' : '📦'; chest.classList.add('shake-box'); urLayer.classList.remove('active'); flashText.classList.add('hidden'); chest.style.transform = 'scale(1)';
    
    // チュートリアル中ならオーバーレイを確実に隠す
    if (!DB.tutorialGachaCompleted) { document.getElementById('tutorial-overlay').classList.add('hidden'); document.getElementById('tutorial-arrow').classList.add('hidden'); }

    setTimeout(() => {
        chest.classList.remove('shake-box');
        if (hasUR) {
            urLayer.classList.add('active'); chest.innerText = '✨🎁✨'; chest.style.transform = 'scale(1.5)'; flashText.classList.remove('hidden');
            if (isFirebaseInitialized && DB.tutorialCompleted) { window.db.ref('global/latest_ur_pull').set({ userId: DB.userId, userName: DB.userName, itemName: highestRarityItem.name, timestamp: firebase.database.ServerValue.TIMESTAMP }); } else { setTimeout(() => { triggerHappyShare(DB.userName, highestRarityItem.name); }, 1000); }
            setTimeout(() => { animCont.classList.add('hidden'); animCont.classList.remove('flex'); if (times === 1) showGachaResult(currentGachaResults[0].item, currentGachaResults[0].isNew); else showGacha10Result(); }, 3000);
        } else {
            chest.innerText = currentGachaResults.some(r=>r.item.rarity==='SR' || r.item.rarity==='SSR') ? '🎁' : '📦';
            setTimeout(() => { animCont.classList.add('hidden'); animCont.classList.remove('flex'); if (times === 1) showGachaResult(currentGachaResults[0].item, currentGachaResults[0].isNew); else showGacha10Result(); }, 1000);
        }
        updateGachaUI(); saveGame(); 
    }, 1500);
}

function showGachaResult(item, isNew) {
    const modal = document.getElementById('gacha-result-modal');
    document.querySelectorAll('.gacha-result-text').forEach(el => { el.style.animation = 'none'; el.offsetHeight; el.style.animation = null; });
    shootConfetti('gacha-confetti-container', 80);
    document.getElementById('gacha-result-rarity').innerText = item.rarity; document.getElementById('gacha-result-rarity').className = `text-7xl font-black mb-2 gacha-result-text italic tracking-wider ${rarityColors[item.rarity]}`;
    const visualContainer = document.getElementById('gacha-result-visual-container'); visualContainer.innerHTML = '';
    if (item.type === 'image') visualContainer.innerHTML = `<span class="inline-flex relative w-24 h-24 filter drop-shadow-[0_0_15px_rgba(255,255,255,0.8)] z-10"><img src="${item.icon}" class="absolute inset-0 w-full h-full object-contain" onerror="fb(this,'${item.fallbackIcon}','text-5xl')"></span>`;
    else visualContainer.innerHTML = `<span class="text-8xl filter drop-shadow-[0_0_15px_rgba(255,255,255,0.8)]">${item.icon || '⚔️'}</span>`;
    document.getElementById('gacha-result-name').innerHTML = `${item.name} <br><span class="text-base text-yellow-400 mt-2 block font-black border-t border-indigo-500 pt-2">${isNew ? '🎉 初ゲット！！' : '持ってるぞ！(コイン+500/化石+1)'}</span>`;
    document.getElementById('gacha-result-desc').innerText = item.desc || '';
    modal.classList.remove('hidden'); modal.classList.add('flex');
}
function closeGachaResult() {
    document.getElementById('gacha-result-modal').classList.add('hidden'); document.getElementById('gacha-result-modal').classList.remove('flex'); document.getElementById('gacha-confetti-container').innerHTML = ''; 
    if (!DB.tutorialGachaCompleted) { DB.tutorialGachaCompleted = true; saveGame(); setTimeout(() => startSpecificTutorial('gacha_done'), 300); }
}
function showGacha10Result() {
    const modal = document.getElementById('gacha-result-10-modal'); const grid = document.getElementById('gacha-10-grid'); grid.innerHTML = ''; shootConfetti('gacha-confetti-container-10', 120);
    currentGachaResults.forEach((res, index) => {
        const item = res.item; const div = document.createElement('div');
        div.className = `bg-indigo-900 border-2 border-indigo-500 rounded-xl p-2 flex flex-col items-center justify-center relative shadow-lg pop-in`; div.style.animationDelay = `${index * 0.1}s`; div.style.animationFillMode = 'both';
        let visualHTML = item.type === 'image' ? `<span class="inline-flex relative w-12 h-12"><img src="${item.icon}" class="absolute inset-0 w-full h-full object-contain drop-shadow-md z-10" onerror="fb(this,'${item.fallbackIcon}','text-2xl drop-shadow-md')"></span>` : `<span class="text-5xl drop-shadow-md">${item.icon || '⚔️'}</span>`;
        div.innerHTML = `<div class="absolute top-1 left-1 ${rarityColors[item.rarity]} font-black text-[10px] bg-indigo-950/80 px-1 rounded">${item.rarity}</div>${res.isNew ? '<div class="absolute top-1 right-1 text-[8px] bg-pink-500 text-white font-black px-1 rounded shadow-md">NEW</div>' : '<div class="absolute top-1 right-1 text-[8px] bg-slate-500 text-white font-black px-1 rounded shadow-md">ダブり</div>'}<div class="mt-4 mb-2 h-12 flex items-center justify-center">${visualHTML}</div><div class="text-[9px] text-white font-bold text-center w-full truncate px-1">${item.name}</div>`;
        grid.appendChild(div);
    });
    modal.classList.remove('hidden'); modal.classList.add('flex');
}
function closeGacha10Result() {
    document.getElementById('gacha-result-10-modal').classList.add('hidden'); document.getElementById('gacha-result-10-modal').classList.remove('flex'); document.getElementById('gacha-confetti-container-10').innerHTML = ''; 
    if (!DB.tutorialGachaCompleted) { DB.tutorialGachaCompleted = true; saveGame(); setTimeout(() => startSpecificTutorial('gacha_done'), 300); }
}

function closeHappyShareToast() { const toast = document.getElementById('happy-share-toast'); toast.classList.remove('show'); if (happyShareTimeoutId) { clearTimeout(happyShareTimeoutId); happyShareTimeoutId = null; } }
function triggerHappyShare(userName, itemName) {
    const toast = document.getElementById('happy-share-toast'); const msg = document.getElementById('toast-message'); if (happyShareTimeoutId) { clearTimeout(happyShareTimeoutId); }
    msg.innerHTML = `<span class="text-yellow-200">${userName}</span> が<br>超激レア【${itemName}】を入手！`; toast.classList.add('show');
    DB.gachaTickets += 1; updateHeaderUI(); saveGame(); 
    happyShareTimeoutId = setTimeout(() => { closeHappyShareToast(); }, 3500);
}


/**
 * 🗺️ ステージ選択・マルチ選択
 */
let targetMultiStage = null;

function switchStageTab(tabType) {
    currentStageTab = tabType;
    const btnNormal = document.getElementById('tab-stage-normal'); const btnEx = document.getElementById('tab-stage-ex'); const btnMulti = document.getElementById('tab-stage-multi');
    [btnNormal, btnEx, btnMulti].forEach(btn => { btn.className = "flex-1 py-2 rounded-lg font-black text-xs text-indigo-400 hover:text-white transition-all border-b-4 border-transparent shadow-sm"; });
    if (tabType === 'normal') btnNormal.className = "flex-1 py-2 rounded-lg font-black text-xs bg-indigo-600 text-white shadow-md transition-all border-b-4 border-indigo-800";
    else if (tabType === 'ex') btnEx.className = "flex-1 py-2 rounded-lg font-black text-xs bg-indigo-600 text-white shadow-md transition-all border-b-4 border-indigo-800 relative";
    else if (tabType === 'multi') btnMulti.className = "flex-1 py-2 rounded-lg font-black text-xs bg-indigo-600 text-white shadow-md transition-all border-b-4 border-indigo-800 relative";
    renderStageList();
}
function initStageSelect() { switchStageTab('normal'); }

function renderStageList() {
    const list = document.getElementById('stage-list'); list.innerHTML = '';
    let stagesToRender = [];
    if (!DB.tutorialCompleted) { stagesToRender = [{ id: 999, name: 'ワクワク初航海！（訓練海域）', goalScore: 3, enemySpeed: 1.5, spawnRate: 80, reward: 0, color: 'border-yellow-400', isEndless: false, isTutorial: true, staminaCost: 0 }]; } 
    else {
        if (currentStageTab === 'normal') stagesToRender = MASTER.stages; else if (currentStageTab === 'ex') stagesToRender = MASTER.exStages; else if (currentStageTab === 'multi') stagesToRender = MASTER.multiStages;
    }

    stagesToRender.forEach((stage, index) => {
        let isLocked = false;
        if (!stage.isTutorial) {
            if (currentStageTab === 'normal') isLocked = index > 0 && DB.clearedStages < index; 
            else if (currentStageTab === 'ex' && stage.reqClearEx !== undefined) isLocked = DB.clearedSeimaroStage < stage.reqClearEx; 
            else if (currentStageTab === 'multi') isLocked = DB.clearedStages < 10; 
        }
        
        const btn = document.createElement('button'); btn.id = `stage-btn-${stage.id}`;
        btn.className = `p-4 rounded-2xl border-l-8 text-left shadow-lg relative overflow-hidden transition-transform transform hover:scale-105 active:scale-95 flex-shrink-0 ${isLocked ? 'bg-indigo-900 border-slate-600 opacity-60 cursor-not-allowed' : 'bg-indigo-800 ' + stage.color}`;
        
        let rewardDisplay = '';
        if (stage.isTutorial) rewardDisplay = `<span class="text-xs text-yellow-300 font-black bg-indigo-950 px-2 py-1 rounded-md">ライセンス アンロック 🔑</span>`;
        else if (stage.isEndless) rewardDisplay = `<span class="text-xs text-yellow-300 font-black bg-indigo-950 px-2 py-1 rounded-md">${stage.reward}</span>`;
        else rewardDisplay = `<div class="flex items-center gap-1 bg-indigo-950 px-2 py-1 rounded-md"><span class="inline-flex relative w-4 h-4"><img src="草核.png" class="absolute inset-0 w-full h-full object-contain bg-transparent z-10" onerror="fb(this,'✨','text-xs')"></span><span class="text-sm text-yellow-400 font-black">×${stage.reward}</span></div>`;

        let typeLabel = '';
        if (stage.isMulti) typeLabel = `<span class="bg-indigo-950 text-[10px] text-white px-2 py-0.5 rounded-full border ${stage.multiType === 'coop' ? 'border-blue-500' : 'border-red-500'} mr-2">${stage.multiType === 'coop' ? '🤝 協力' : '⚔️ 対戦'}</span>`;

        btn.innerHTML = `
            <div class="absolute top-2 right-2 text-xs font-black text-yellow-300 bg-indigo-950 px-2 py-0.5 rounded-full shadow border border-indigo-700">⚡ ${stage.staminaCost}</div>
            <div class="font-black text-xl text-white mb-2 pr-12 drop-shadow-md truncate flex items-center">${typeLabel}${stage.name}</div>
            <div class="text-sm text-indigo-200 font-bold bg-indigo-950/50 inline-block px-2 py-0.5 rounded-full mb-2">目標: ${stage.goalScore}${stage.isEndless || stage.isBossStage ? '' : '隻 ド派手に追い払う！'}</div>
            <div class="flex items-center gap-2 mt-1"><span class="text-xs text-indigo-300 font-bold">${stage.isEndless || stage.isBossStage ? '報酬:' : stage.isTutorial ? '目標報酬:' : '初回お祝い:'}</span>${rewardDisplay}</div>
            ${isLocked ? `<div class="absolute inset-0 bg-indigo-950/80 flex items-center justify-center font-black text-3xl tracking-widest text-indigo-400 backdrop-blur-sm z-20">${currentStageTab === 'multi' ? '通常海域10クリアで解放 🔒' : 'LOCKED 🔒'}</div>` : ''}
        `;
        
        if (!isLocked) {
            btn.onclick = () => { 
                positionTutorialArrow(null); 
                if (stage.isMulti) { openFriendSelectModal(stage); } else { startStage(stage); }
            };
        }
        list.appendChild(btn);
    });

    if (!DB.tutorialCompleted) setTimeout(() => { positionTutorialArrow('stage-btn-999'); }, 100);
}

// 🤝 フレンド選択モーダルを開く
function openFriendSelectModal(stage) {
    targetMultiStage = stage;
    const modal = document.getElementById('friend-select-modal');
    const listCont = document.getElementById('friend-select-list');
    listCont.innerHTML = '';

    // フレンドデータが空の場合でも、CPUダミー「ポリー」を用意する
    let displayFriends = Object.values(cachedFriendData);
    
    // もしフレンドが0人ならポリーを追加
    if (displayFriends.length === 0) {
        displayFriends.push({
            userName: 'ポリー船長 (CPU)', userIcon: '🦜',
            equippedWeapon: 'wp_machine', equippedSkin: 'skin_pirate', equippedCrew: 'crew_score',
            itemLevels: { 'wp_machine': 5, 'skin_pirate': 5, 'crew_score': 5 }
        });
    }

    displayFriends.forEach(friend => {
        const btn = document.createElement('button');
        btn.className = "game-btn w-full bg-indigo-900 border-2 border-indigo-500 rounded-xl p-3 flex items-center gap-3 text-left transition-transform hover:bg-indigo-800 active:scale-95";
        
        let iconHtml = '';
        if (friend.userIcon && friend.userIcon.startsWith('data:image')) {
            iconHtml = `<img src="${friend.userIcon}" class="w-12 h-12 rounded-xl object-cover border-2 border-indigo-400 shadow-md">`;
        } else {
            iconHtml = `<div class="w-12 h-12 bg-indigo-950 rounded-xl flex items-center justify-center text-2xl border-2 border-indigo-400 shadow-md">${friend.userIcon || '👤'}</div>`;
        }
        
        btn.innerHTML = `
            ${iconHtml}
            <div class="flex-1 overflow-hidden">
                <div class="font-black text-white text-base truncate">${friend.userName}</div>
                <div class="text-[10px] text-indigo-300 font-bold mt-1">選んで出航する！</div>
            </div>
            <div class="text-2xl">➡️</div>
        `;
        
        btn.onclick = () => {
            closeFriendSelectModal();
            startStage(targetMultiStage, friend);
        };
        listCont.appendChild(btn);
    });

    modal.classList.remove('hidden');
    modal.classList.add('flex');
}

function closeFriendSelectModal() {
    document.getElementById('friend-select-modal').classList.add('hidden');
    document.getElementById('friend-select-modal').classList.remove('flex');
    targetMultiStage = null;
}


/**
 * 💥 Canvas ゲームエンジン
 */
const canvas = document.getElementById('gameCanvas'); const ctx = canvas.getContext('2d');
let gameLoopId = null; let currentStage = null; let specialCutinTimeout = null;

// ★フレンド（味方・敵）情報を gameState に追加
let gameState = { score: 0, frameCount: 0, player: {}, ally: null, bullets: [], enemies: [], enemyBullets: [], particles: [], bgOffset: 0, specialGauge: 0, isPaused: false, screenFlash: 0, bossDefeated: false };
const input = { touchX: 0, touchY: 0, isTouching: false, keys: {} };
window.addEventListener('keydown', e => input.keys[e.key] = true); window.addEventListener('keyup', e => input.keys[e.key] = false);

canvas.addEventListener('touchstart', e => { input.isTouching = true; const rect = canvas.getBoundingClientRect(); input.touchX = e.touches[0].clientX - rect.left; });
canvas.addEventListener('touchmove', e => { if (!input.isTouching) return; e.preventDefault(); const rect = canvas.getBoundingClientRect(); const cx = e.touches[0].clientX - rect.left; if (!gameState.isPaused) gameState.player.x += (cx - input.touchX); input.touchX = cx; });
canvas.addEventListener('touchend', () => input.isTouching = false);

function startStage(stage, friendData = null) {
    if (DB.stamina < stage.staminaCost) { showAlert(`スタミナが足りないぞ！\n時間で回復するか、草コインで回復しよう！\n(必要スタミナ: ${stage.staminaCost})`); return; }
    DB.stamina -= stage.staminaCost; saveGame(); updateHeaderUI();

    currentStage = stage; navTo('screen-game');
    if (DB.tutorialCompleted) document.getElementById('special-ui-container').classList.remove('hidden'); else document.getElementById('special-ui-container').classList.add('hidden');

    setTimeout(() => {
        const rect = canvas.parentElement.getBoundingClientRect(); let computedWidth = rect.width; let computedHeight = rect.height;
        if (computedWidth < 100) computedWidth = window.innerWidth; if (computedHeight < 100) computedHeight = window.innerHeight - 80;
        canvas.width = computedWidth; canvas.height = computedHeight;

        // --- プレイヤーの設定 ---
        const weapon = MASTER.weapons[DB.equippedWeapon]; const skin = MASTER.skins[DB.equippedSkin]; const crew = MASTER.crew[DB.equippedCrew];
        const wLevel = getItemLevel(weapon.id); const sLevel = getItemLevel(skin.id); const cLevel = getItemLevel(crew.id);

        gameState = {
            score: 0, frameCount: 0, bullets: [], enemies: [], enemyBullets: [], particles: [], bgOffset: 0, specialGauge: 0, isPaused: false, screenFlash: 0, bossDefeated: false,
            player: {
                x: canvas.width / 2 - 30, y: canvas.height - 140, width: 60, height: 60, speed: 6 + (crew.effect === 'speedUp' ? 4 : 0) + (sLevel * 0.5), skinIcon: skin.icon,
                weapon: { ...weapon, fireRate: Math.max(5, weapon.fireRate - Math.floor(wLevel / 2)), speed: weapon.speed + (wLevel * 0.5) },
                crewId: crew.id, crewIcon: crew.icon, crewFallback: crew.fallbackIcon, crewType: crew.type, crewSkillName: crew.skillName, crewEffect: crew.effect,
                bulletSizeMod: (crew.effect === 'powerUp' ? 2 : 1) + (cLevel * 0.1), scoreMod: (crew.effect === 'scoreUp' ? 2 : 1) + (cLevel * 0.5),
                hp: 5, maxHp: 5 
            },
            ally: null
        };

        // --- 🤝 マルチプレイ（フレンドデータ）の反映 ---
        if (stage.isMulti && friendData) {
            const fWpId = friendData.equippedWeapon || 'wp_normal';
            const fSkinId = friendData.equippedSkin || 'skin_default';
            const fCrewId = friendData.equippedCrew || 'crew_none';
            const fLvW = (friendData.itemLevels && friendData.itemLevels[fWpId]) || 1;
            const fLvS = (friendData.itemLevels && friendData.itemLevels[fSkinId]) || 1;
            const fLvC = (friendData.itemLevels && friendData.itemLevels[fCrewId]) || 1;
            
            const fWp = MASTER.weapons[fWpId] || MASTER.weapons['wp_normal'];
            const fSkin = MASTER.skins[fSkinId] || MASTER.skins['skin_default'];
            const fCrew = MASTER.crew[fCrewId] || MASTER.crew['crew_none'];

            let parsedFriend = {
                name: friendData.userName || 'フレンド',
                width: 60, height: 60,
                skinIcon: fSkin.icon,
                weapon: { ...fWp, fireRate: Math.max(5, fWp.fireRate - Math.floor(fLvW / 2)), speed: fWp.speed + (fLvW * 0.5) },
                crewEffect: fCrew.effect,
                bulletSizeMod: (fCrew.effect === 'powerUp' ? 2 : 1) + (fLvC * 0.1)
            };

            if (stage.multiType === 'coop') {
                // 共闘: 味方として配置
                parsedFriend.x = gameState.player.x + 80;
                parsedFriend.y = gameState.player.y + 20;
                gameState.ally = parsedFriend;
            } else if (stage.multiType === 'pvp') {
                // 対戦: ボス敵として配置
                parsedFriend.x = canvas.width / 2 - 40;
                parsedFriend.y = -100;
                parsedFriend.w = 80; parsedFriend.h = 80; // ボスなので少し大きく
                parsedFriend.hp = 1000 + (fLvW + fLvS + fLvC) * 50; // 装備レベルでHP増強
                parsedFriend.maxHp = parsedFriend.hp;
                parsedFriend.speed = 2 + (fLvS * 0.1);
                parsedFriend.isBoss = true;
                parsedFriend.isPlayerRival = true;
                parsedFriend.wobbleOffset = 0;
                gameState.enemies.push(parsedFriend);
            }
        }

        document.getElementById('game-score').innerText = '0'; document.getElementById('game-goal').innerText = stage.goalScore; document.getElementById('game-end-modal').classList.add('hidden'); updateSpecialGaugeUI(); 

        if (stage.isBossStage && stage.id === 'ex_seimaro_3') {
            document.getElementById('player-hp-container').classList.remove('hidden'); updatePlayerHpUI();
            gameState.enemies.push({ x: canvas.width / 2 - 75, y: -150, w: 150, h: 150, speed: stage.enemySpeed, hp: 750, maxHp: 750, wobbleOffset: 0, isBoss: true });
        } else if (stage.multiType === 'pvp') {
            document.getElementById('player-hp-container').classList.remove('hidden'); updatePlayerHpUI();
        } else {
            document.getElementById('player-hp-container').classList.add('hidden');
        }

        const guideOverlay = document.getElementById('game-tutorial-overlay');
        if (stage.isTutorial) {
            guideOverlay.classList.remove('hidden'); guideOverlay.style.opacity = 1; document.getElementById('game-tutorial-text').innerText = '左右にスライド、または矢印キーで船を動かそう！大砲は自動で連射されるぞ！';
            setTimeout(() => { guideOverlay.style.opacity = 0; setTimeout(() => { guideOverlay.classList.add('hidden'); guideOverlay.style.opacity = ''; }, 1000); }, 4000);
        } else { guideOverlay.classList.add('hidden'); }

        if (gameLoopId) cancelAnimationFrame(gameLoopId); gameLoop();
    }, 50);
}

function stopGame() { if (gameLoopId) { cancelAnimationFrame(gameLoopId); gameLoopId = null; } if (specialCutinTimeout) { clearTimeout(specialCutinTimeout); specialCutinTimeout = null; } document.getElementById('special-cutin').classList.add('hidden'); }

function triggerGameOver(reasonStr = "ライバルに防衛ラインを突破されてしまった...！") {
    stopGame(); const modal = document.getElementById('game-end-modal');
    document.getElementById('game-end-title').innerText = 'GAME OVER...'; document.getElementById('game-end-title').className = 'text-5xl font-black mb-4 text-center text-transparent bg-clip-text bg-gradient-to-b from-red-400 to-red-600 filter drop-shadow-lg transform scale-110 pop-in relative z-10';
    let desc = `${reasonStr}\n\n今回の追い払った数: ${gameState.score}隻\n\n<span class="text-sm text-yellow-300">※放置はダメだぞ！しっかり船を操作して敵を逃さないようにしよう！</span>`;
    document.getElementById('game-end-desc').innerHTML = desc; modal.classList.remove('hidden'); modal.classList.add('flex'); document.getElementById('end-confetti-container').innerHTML = '';
    setTimeout(() => { positionTutorialArrow('btn-tutorial-exit'); }, 300);
}

let lastGaugeUI = -1;
function updateSpecialGaugeUI() {
    let g = Math.floor(gameState.specialGauge); if (g === lastGaugeUI) return; lastGaugeUI = g;
    const bar = document.getElementById('special-gauge'); bar.style.width = `${g}%`; const btn = document.getElementById('btn-special'); const icon = document.getElementById('btn-special-icon');
    if (g >= 100) { btn.classList.remove('bg-slate-700', 'border-slate-500', 'opacity-80', 'cursor-not-allowed'); btn.classList.add('bg-gradient-to-t', 'from-yellow-400', 'to-orange-500', 'border-white', 'animate-pulse'); icon.classList.remove('grayscale'); btn.disabled = false; } 
    else { btn.classList.add('bg-slate-700', 'border-slate-500', 'opacity-80', 'cursor-not-allowed'); btn.classList.remove('bg-gradient-to-t', 'from-yellow-400', 'to-orange-500', 'border-white', 'animate-pulse'); icon.classList.add('grayscale'); btn.disabled = true; }
}

function updatePlayerHpUI() {
    const p = gameState.player;
    document.getElementById('player-hp-text').innerText = `${p.hp}/${p.maxHp}`;
    document.getElementById('player-hp-bar').style.width = `${(p.hp / p.maxHp) * 100}%`;
}

function triggerSpecialSkill() {
    if (gameState.specialGauge < 100 || gameState.isPaused) return; gameState.isPaused = true; 
    const p = gameState.player; const cutin = document.getElementById('special-cutin'); const cutinChar = document.getElementById('cutin-char'); const cutinName = document.getElementById('cutin-name'); const flash = document.getElementById('cutin-flash');
    
    if (p.crewType === 'image') cutinChar.innerHTML = `<span class="inline-flex relative w-48 h-44 filter drop-shadow-[0_0_25px_#38bdf8]"><img src="${p.crewIcon}" class="absolute inset-0 w-full h-full object-contain z-10" onerror="fb(this,'${p.crewFallback}','text-[10rem]')"></span>`;
    else cutinChar.innerHTML = `<span class="text-[10rem] filter drop-shadow-[0_0_20px_#38bdf8]">${p.crewIcon}</span>`;
    
    cutinName.innerText = p.crewSkillName || 'フルパワー全開！'; cutin.classList.remove('hidden');
    cutinChar.classList.remove('cutin-char-anim'); cutinName.classList.remove('cutin-name-anim'); flash.classList.remove('flash-anim'); void cutinChar.offsetWidth; 
    cutinChar.classList.add('cutin-char-anim'); cutinName.classList.add('cutin-name-anim');
    if (p.crewId === 'crew_seimaro') flash.classList.add('flash-anim');
    specialCutinTimeout = setTimeout(() => { executeSpecialEffect(); }, 2500); 
}

function skipSpecial() { if (specialCutinTimeout) { clearTimeout(specialCutinTimeout); specialCutinTimeout = null; executeSpecialEffect(); } }

function executeSpecialEffect() {
    document.getElementById('special-cutin').classList.add('hidden'); const p = gameState.player;
    
    if (p.crewId === 'crew_seimaro') {
        gameState.enemies.forEach(e => {
            if (e.isBoss) { e.hp -= 150; createPopParticles(e.x + e.w/2, e.y + e.h/2, '#0ea5e9', true); } 
            else { createPopParticles(e.x + e.w/2, e.y + e.h/2, '#0ea5e9', true); createPopParticles(e.x + e.w/2, e.y + e.h/2, '#ffffff', false); gameState.score += Math.floor(1 * p.scoreMod); e.hp = 0; }
        });
        gameState.screenFlash = 25; 
    } else {
        gameState.enemies.forEach(e => {
            if (e.isBoss) { e.hp -= 50; createPopParticles(e.x + e.w/2, e.y + e.h/2, '#ffffff', true); } 
            else { createPopParticles(e.x + e.w/2, e.y + e.h/2, '#ffffff', true); gameState.score += Math.floor(1 * p.scoreMod); e.hp = 0; }
        });
        gameState.screenFlash = 5; 
    }
    gameState.enemies = gameState.enemies.filter(e => e.hp > 0); document.getElementById('game-score').innerText = gameState.score;
    gameState.specialGauge = 0; updateSpecialGaugeUI(); gameState.isPaused = false;
    if (currentStage.isBossStage && gameState.enemies.length === 0) gameState.bossDefeated = true;
    checkStageClear(false);
}

function gameLoop() { if (!gameState.isPaused) updateGame(); drawGame(); gameLoopId = requestAnimationFrame(gameLoop); }

function updateGame() {
    const p = gameState.player; const s = currentStage; gameState.frameCount++; gameState.bgOffset = (gameState.bgOffset + 2) % 100;

    if (s.isEndless) { s.enemySpeed = 3 + (gameState.score / 50); s.spawnRate = Math.max(10, 30 - Math.floor(gameState.score / 20)); }

    if (gameState.specialGauge < 100) { gameState.specialGauge += 0.05; if (gameState.specialGauge > 100) gameState.specialGauge = 100; if (gameState.frameCount % 5 === 0) updateSpecialGaugeUI(); }

    if (input.keys['ArrowLeft'] || input.keys['a']) p.x -= p.speed; if (input.keys['ArrowRight'] || input.keys['d']) p.x += p.speed;
    p.x = Math.max(0, Math.min(canvas.width - p.width, p.x));

    // 🔴 プレイヤーの弾発射
    if (gameState.frameCount % p.weapon.fireRate === 0) {
        const bx = p.x + p.width / 2; const by = p.y + 10; const bw = 12 * p.bulletSizeMod; const bh = (p.weapon.type === 'laser' ? 100 : 20) * p.bulletSizeMod;
        if (p.crewEffect === 'doubleShot') {
            gameState.bullets.push({x: p.x+5-bw/2, y: by, w: bw, h: bh, dx: 0, dy: -p.weapon.speed, color: p.weapon.color, type: p.weapon.type});
            gameState.bullets.push({x: p.x+p.width-5-bw/2, y: by, w: bw, h: bh, dx: 0, dy: -p.weapon.speed, color: p.weapon.color, type: p.weapon.type});
        } else if (p.weapon.type === 'spread') {
            gameState.bullets.push({x: bx-bw/2, y: by, w: bw, h: bh, dx: 0, dy: -p.weapon.speed, color: p.weapon.color, type: 'normal'});
            gameState.bullets.push({x: bx-bw/2, y: by, w: bw, h: bh, dx: -3, dy: -p.weapon.speed, color: p.weapon.color, type: 'normal'});
            gameState.bullets.push({x: bx-bw/2, y: by, w: bw, h: bh, dx: 3, dy: -p.weapon.speed, color: p.weapon.color, type: 'normal'});
        } else {
            gameState.bullets.push({x: bx-bw/2, y: by, w: bw, h: bh, dx: 0, dy: -p.weapon.speed, color: p.weapon.color, type: p.weapon.type});
        }
    }

    // 🤝 味方（Co-op）の更新と弾発射
    if (gameState.ally) {
        let a = gameState.ally;
        a.x += (p.x + 80 - a.x) * 0.05; // プレイヤーの少し右を追従
        a.y += (p.y + 20 - a.y) * 0.05;

        if (gameState.frameCount % a.weapon.fireRate === 0) {
            const bx = a.x + a.width / 2; const by = a.y + 10; const bw = 12 * a.bulletSizeMod; const bh = (a.weapon.type === 'laser' ? 100 : 20) * a.bulletSizeMod;
            if (a.crewEffect === 'doubleShot') {
                gameState.bullets.push({x: a.x+5-bw/2, y: by, w: bw, h: bh, dx: 0, dy: -a.weapon.speed, color: a.weapon.color, type: a.weapon.type});
                gameState.bullets.push({x: a.x+a.width-5-bw/2, y: by, w: bw, h: bh, dx: 0, dy: -a.weapon.speed, color: a.weapon.color, type: a.weapon.type});
            } else if (a.weapon.type === 'spread') {
                gameState.bullets.push({x: bx-bw/2, y: by, w: bw, h: bh, dx: 0, dy: -a.weapon.speed, color: a.weapon.color, type: 'normal'});
                gameState.bullets.push({x: bx-bw/2, y: by, w: bw, h: bh, dx: -3, dy: -a.weapon.speed, color: a.weapon.color, type: 'normal'});
                gameState.bullets.push({x: bx-bw/2, y: by, w: bw, h: bh, dx: 3, dy: -a.weapon.speed, color: a.weapon.color, type: 'normal'});
            } else {
                gameState.bullets.push({x: bx-bw/2, y: by, w: bw, h: bh, dx: 0, dy: -a.weapon.speed, color: a.weapon.color, type: a.weapon.type});
            }
        }
    }

    for (let i = gameState.bullets.length - 1; i >= 0; i--) { let b = gameState.bullets[i]; b.x += b.dx; b.y += b.dy; if (b.y < -100 || b.x < -50 || b.x > canvas.width + 50) gameState.bullets.splice(i, 1); }

    // 🔴 敵の生成
    if (!s.isBossStage && gameState.frameCount % s.spawnRate === 0) {
        gameState.enemies.push({ x: Math.random() * (canvas.width - 50), y: -50, w: 50, h: 50, speed: s.enemySpeed + (Math.random() * 1.5), hp: 1, wobbleOffset: Math.random() * Math.PI * 2 });
    } else if (s.isBossStage && gameState.frameCount % (s.spawnRate / 10) === 0 && s.id === 'ex_seimaro_3') {
        gameState.enemies.push({ x: Math.random() * (canvas.width - 50), y: -50, w: 50, h: 50, speed: 3 + (Math.random() * 1.5), hp: 1, wobbleOffset: Math.random() * Math.PI * 2 });
    }

    for (let i = gameState.enemies.length - 1; i >= 0; i--) {
        let e = gameState.enemies[i];
        if (e.isBoss) { 
            if (e.y < 50) e.y += e.speed; 
            else {
                // PvPボスはプレイヤーを少し追尾するような動き
                if (e.isPlayerRival) {
                    e.x += (p.x - e.x) * 0.02 + Math.sin(gameState.frameCount / 20) * 2;
                } else {
                    e.x += Math.sin(gameState.frameCount / 30) * 2; 
                }
            }
            
            // ボス攻撃
            let attackRate = e.isPlayerRival ? (e.weapon ? e.weapon.fireRate * 2 : 40) : 40;
            if (gameState.frameCount % attackRate === 0) {
                // PvPの時は相手の武器色にする
                let bColor = e.isPlayerRival && e.weapon ? e.weapon.color : '#ef4444';
                gameState.enemyBullets.push({ x: e.x + e.w / 2 - 8, y: e.y + e.h - 10, w: 16, h: 16, dx: 0, dy: 6, color: bColor });
                
                if (e.isPlayerRival && e.crewEffect === 'doubleShot') {
                    gameState.enemyBullets.push({ x: e.x + 10, y: e.y + e.h - 10, w: 16, h: 16, dx: 0, dy: 6, color: bColor });
                    gameState.enemyBullets.push({ x: e.x + e.w - 10, y: e.y + e.h - 10, w: 16, h: 16, dx: 0, dy: 6, color: bColor });
                }
            }
        } 
        else { e.y += e.speed; e.x += Math.sin(gameState.frameCount / 20 + e.wobbleOffset) * 1.5; }

        if (checkRectCollision(p, e)) { if (!e.isBoss) { gameState.enemies.splice(i, 1); createPopParticles(e.x + e.w/2, e.y + e.h/2, '#38bdf8'); } continue; }

        let hit = false;
        for (let j = gameState.bullets.length - 1; j >= 0; j--) {
            let b = gameState.bullets[j];
            if (checkRectCollision(b, e)) { hit = true; e.hp--; if (b.type !== 'laser') gameState.bullets.splice(j, 1); if (e.hp <= 0) break; }
        }

        if (hit && e.hp <= 0) {
            gameState.enemies.splice(i, 1); createPopParticles(e.x + e.w/2, e.y + e.h/2, '#facc15', true); 
            if (e.isBoss) { gameState.bossDefeated = true; gameState.score += 1; } else { gameState.score += Math.floor(1 * p.scoreMod); }
            document.getElementById('game-score').innerText = gameState.score;
            
            if (gameState.specialGauge < 100) { gameState.specialGauge += (e.isBoss ? 50 : 5); if (gameState.specialGauge > 100) gameState.specialGauge = 100; updateSpecialGaugeUI(); }

            checkStageClear(false);
        } else if (!e.isBoss && canvas.height > 200 && e.y + e.h > canvas.height - 50) {
            triggerGameOver(); return;
        }
    }

    if (s.isBossStage || s.multiType === 'pvp') {
        for (let i = gameState.enemyBullets.length - 1; i >= 0; i--) {
            let eb = gameState.enemyBullets[i];
            eb.x += eb.dx; eb.y += eb.dy;
            if (checkRectCollision(eb, p)) {
                gameState.enemyBullets.splice(i, 1); p.hp--; updatePlayerHpUI();
                createPopParticles(p.x + p.width/2, p.y + p.height/2, '#ef4444');
                if (p.hp <= 0) { triggerGameOver("ボスの攻撃を受けて船が沈没してしまった...！"); return; }
            } else if (eb.y > canvas.height) { gameState.enemyBullets.splice(i, 1); }
        }
    }

    for (let i = gameState.particles.length - 1; i >= 0; i--) { let pt = gameState.particles[i]; pt.x += pt.dx; pt.y += pt.dy; pt.life--; pt.dy += 0.2; if (pt.life <= 0) gameState.particles.splice(i, 1); }
}

function drawGame() {
    ctx.clearRect(0, 0, canvas.width, canvas.height);

    ctx.strokeStyle = 'rgba(255, 255, 255, 0.1)';
    ctx.lineWidth = 2;
    for(let y = (gameState.bgOffset % 100) - 100; y < canvas.height; y += 100) {
        ctx.beginPath();
        for(let x = 0; x < canvas.width; x += 50) { ctx.quadraticCurveTo(x + 25, y + 10, x + 50, y); }
        ctx.stroke();
    }

    const p = gameState.player;
    if (p.crewId === 'crew_seimaro' && !gameState.isPaused) {
        if (gameState.frameCount % 2 === 0) { 
            const originX = p.x + p.width / 2; const originY = p.y + p.height / 2;
            for(let lightningCount=0; lightningCount<3; lightningCount++) {
                ctx.save(); ctx.beginPath();
                let startX = originX + (Math.random() - 0.5) * p.width * 0.5; let startY = originY + (Math.random() - 0.5) * p.height * 0.5; ctx.moveTo(startX, startY);
                for(let seg=0; seg<5; seg++) { 
                    startX += (Math.random() - 0.5) * 60; startY += (Math.random() - 0.5) * 60; ctx.lineTo(startX, startY);
                    if (Math.random() > 0.5) { ctx.fillStyle = '#cffafe'; ctx.beginPath(); ctx.arc(startX, startY, Math.random() * 5 + 2, 0, Math.PI*2); ctx.fill(); }
                }
                const colors = ['#ffffff', '#38bdf8', '#22d3ee']; ctx.strokeStyle = colors[Math.floor(Math.random() * colors.length)]; 
                ctx.lineWidth = Math.random() * 4 + 2; ctx.shadowColor = '#0284c7'; ctx.shadowBlur = 25; ctx.lineCap = 'round'; ctx.lineJoin = 'round'; ctx.stroke(); ctx.restore();
            }
        }
    }

    ctx.save(); ctx.strokeStyle = 'rgba(239, 68, 68, 0.6)'; ctx.lineWidth = 3; ctx.setLineDash([10, 10]); ctx.beginPath(); ctx.moveTo(0, canvas.height - 50); ctx.lineTo(canvas.width, canvas.height - 50); ctx.stroke(); ctx.fillStyle = 'rgba(239, 68, 68, 0.2)'; ctx.fillRect(0, canvas.height - 50, canvas.width, 50); ctx.restore();

    gameState.bullets.forEach(b => {
        ctx.fillStyle = b.color; ctx.shadowColor = b.color; ctx.shadowBlur = 15;
        if(b.type === 'laser') { ctx.fillRect(b.x, b.y, b.w, b.h); } 
        else { ctx.beginPath(); ctx.ellipse(b.x + b.w/2, b.y + b.h/2, b.w/2, b.h/2, 0, 0, Math.PI*2); ctx.fill(); ctx.fillStyle = 'rgba(255,255,255,0.8)'; ctx.beginPath(); ctx.arc(b.x + b.w/2 - b.w/6, b.y + b.h/2 - b.h/6, b.w/4, 0, Math.PI*2); ctx.fill(); }
    });
    ctx.shadowBlur = 0;
    
    if (gameState.enemyBullets) {
        gameState.enemyBullets.forEach(eb => {
            ctx.fillStyle = eb.color; ctx.shadowColor = eb.color; ctx.shadowBlur = 10;
            ctx.beginPath(); ctx.arc(eb.x + eb.w/2, eb.y + eb.h/2, eb.w/2, 0, Math.PI*2); ctx.fill();
            ctx.fillStyle = '#fff'; ctx.beginPath(); ctx.arc(eb.x + eb.w/2 - eb.w/4, eb.y + eb.h/2 - eb.h/4, eb.w/4, 0, Math.PI*2); ctx.fill();
        });
        ctx.shadowBlur = 0;
    }

    ctx.textBaseline = "middle"; ctx.textAlign = "center";
    const bossImgElement = document.getElementById('img-seimaro-boss');
    
    gameState.enemies.forEach(e => {
        ctx.save(); ctx.translate(e.x + e.w/2, e.y + e.h/2);
        if (e.isBoss) {
            ctx.rotate(Math.sin(gameState.frameCount / 20) * 0.1);
            if (e.isPlayerRival) {
                // PvPボスの描画（フレンドスキン）
                ctx.font = `${e.w * 0.9}px serif`;
                ctx.fillText(e.skinIcon || '🚢', 0, 0);
                ctx.font = "12px sans-serif"; ctx.fillStyle = "white"; ctx.fillText(e.name, 0, -e.h/2 - 15);
            } else {
                if (bossImgElement.complete && bossImgElement.naturalHeight !== 0) ctx.drawImage(bossImgElement, -e.w/2, -e.h/2, e.w, e.h); else { ctx.font = `${e.w * 0.8}px serif`; ctx.fillText('🐾', 0, 0); }
            }
            ctx.fillStyle = 'rgba(0,0,0,0.8)'; ctx.fillRect(-e.w/2, -e.h/2 - 20, e.w, 10); ctx.fillStyle = '#ef4444'; ctx.fillRect(-e.w/2, -e.h/2 - 20, e.w * (e.hp / e.maxHp), 10); ctx.strokeStyle = '#ffffff'; ctx.lineWidth = 1; ctx.strokeRect(-e.w/2, -e.h/2 - 20, e.w, 10);
        } else {
            ctx.font = "40px serif"; ctx.rotate(Math.sin(gameState.frameCount / 10 + e.wobbleOffset) * 0.2); ctx.fillText('🚢', 0, 0);
        }
        ctx.restore();
    });

    // 🤝 味方の描画
    if (gameState.ally) {
        const a = gameState.ally;
        ctx.save();
        ctx.translate(a.x + a.width/2, a.y + a.height/2);
        ctx.rotate(Math.sin(gameState.frameCount / 15) * 0.05);
        ctx.font = `${a.width}px serif`;
        ctx.fillText(a.skinIcon, 0, 0);
        ctx.font = "10px sans-serif"; ctx.fillStyle = "#facc15"; ctx.fillText(a.name, 0, a.height/2 + 15);
        ctx.restore();
    }

    ctx.font = `${p.width}px serif`; ctx.save(); ctx.translate(p.x + p.width/2, p.y + p.height/2); ctx.rotate(Math.sin(gameState.frameCount / 15) * 0.05); ctx.fillText(p.skinIcon, 0, 0); ctx.restore();
    
    gameState.particles.forEach(pt => {
        ctx.globalAlpha = pt.life / 30;
        if (pt.isStar) drawStar(ctx, pt.x, pt.y, 5, pt.size, pt.size/2, pt.color); else { ctx.fillStyle = pt.color; ctx.beginPath(); ctx.arc(pt.x, pt.y, pt.size, 0, Math.PI * 2); ctx.fill(); }
    });
    ctx.globalAlpha = 1.0;

    if (gameState.screenFlash > 0) {
        const flashProgress = gameState.screenFlash / 25;
        if (p.crewId === 'crew_seimaro') {
            ctx.fillStyle = `rgba(0, 10, 30, ${flashProgress * 0.8})`; ctx.fillRect(0, 0, canvas.width, canvas.height);
            if (gameState.screenFlash > 10) {
                for(let i=0; i<6; i++) {
                    const targetX = Math.random() * canvas.width; const topX = targetX + (Math.random() - 0.5) * 200;
                    drawLightningLine(ctx, topX, 0, targetX, canvas.height, '#0284c7', 20, 50, true); drawLightningLine(ctx, topX, 0, targetX, canvas.height, '#22d3ee', 10, 20, true); drawLightningLine(ctx, topX, 0, targetX, canvas.height, '#ffffff', 4, 10, false);
                    shootLightningImpactConfetti(targetX, canvas.height - 40);
                }
                ctx.fillStyle = `rgba(255, 255, 255, ${Math.random() * 0.5 + 0.3})`; ctx.fillRect(0, 0, canvas.width, canvas.height);
            } else { ctx.fillStyle = `rgba(255, 255, 255, ${(gameState.screenFlash / 10) * 0.5})`; ctx.fillRect(0, 0, canvas.width, canvas.height); }
        } else { ctx.fillStyle = `rgba(255, 255, 255, ${gameState.screenFlash / 5})`; ctx.fillRect(0, 0, canvas.width, canvas.height); }
        gameState.screenFlash--;
    }
}

function checkStageClear(isRetreat = false) {
    const isCleared = currentStage.isBossStage ? gameState.bossDefeated : (!currentStage.isEndless && gameState.score >= currentStage.goalScore);

    if (isRetreat || isCleared) {
        stopGame();
        if (currentStage.isTutorial && !isRetreat) { handleTutorialClear(); return; }

        const modal = document.getElementById('game-end-modal');
        document.getElementById('game-end-title').innerText = isRetreat ? '港に戻りました' : '大・大・大成功！！';
        document.getElementById('game-end-title').className = 'text-4xl font-black mb-4 text-center text-transparent bg-clip-text bg-gradient-to-b from-yellow-200 to-yellow-500 filter drop-shadow-lg transform scale-110 pop-in relative z-10';
        
        let rewardText = `追い払ったライバルの数: ${gameState.score}`; let earnedTickets = 0; let earnedCoins = 0; let earnedMats = 0;

        if (currentStage.isEndless) {
            earnedTickets = Math.floor(gameState.score / 20); earnedCoins = Math.floor(gameState.score / 5) * 50; earnedMats = Math.floor(gameState.score / 20);      
            if (earnedTickets > 0 || earnedCoins > 0) {
                DB.gachaTickets += earnedTickets; DB.coins += earnedCoins; DB.materials += earnedMats;
                rewardText += `<br><br>🎊 探索ご褒美 🎊<br><div class="flex justify-center gap-2 mt-2">
                    ${earnedTickets > 0 ? `<div class="flex items-center gap-1 bg-indigo-950 px-2 py-1 rounded-lg border border-indigo-700"><span class="inline-flex relative w-4 h-4"><img src="草核.png" class="absolute inset-0 w-full h-full object-contain bg-transparent z-10" onerror="fb(this,'✨','text-xs')"></span><span class="text-yellow-400 font-black">×${earnedTickets}</span></div>` : ''}
                    ${earnedCoins > 0 ? `<div class="flex items-center gap-1 bg-indigo-950 px-2 py-1 rounded-lg border border-indigo-700"><span class="inline-flex relative w-4 h-4"><img src="草コイン.png" class="absolute inset-0 w-full h-full object-contain bg-transparent z-10" onerror="fb(this,'💰','text-xs')"></span><span class="text-yellow-400 font-black">×${earnedCoins}</span></div>` : ''}
                    ${earnedMats > 0 ? `<div class="flex items-center gap-1 bg-indigo-950 px-2 py-1 rounded-lg border border-indigo-700"><span class="inline-flex relative w-4 h-4"><img src="草の化石.png" class="absolute inset-0 w-full h-full object-contain bg-transparent z-10" onerror="fb(this,'🦴','text-xs')"></span><span class="text-yellow-400 font-black">×${earnedMats}</span></div>` : ''}
                </div>`;
            } else rewardText += `<br><br><span class="text-sm text-indigo-300">たくさんお見送りするとご褒美を獲得できます</span>`;
        } else {
            let isFirstClear = false;
            if (currentStage.isEx) { if (DB.clearedSeimaroStage < currentStage.reqClearEx + 1 && !isRetreat) { DB.clearedSeimaroStage = currentStage.reqClearEx + 1; isFirstClear = true; } } 
            else if (currentStage.isMulti) { isFirstClear = true; /* マルチはクリア毎に報酬 */ }
            else { if (DB.clearedStages < currentStage.id && !isRetreat) { DB.clearedStages = currentStage.id; isFirstClear = true; } }

            if (isFirstClear && !isRetreat) {
                earnedTickets = currentStage.reward; earnedCoins = currentStage.rewardCoins || 1000; earnedMats = currentStage.rewardMats || 2;
                DB.gachaTickets += earnedTickets; DB.coins += earnedCoins; DB.materials += earnedMats;
                rewardText += `<br><br>🎊 財宝ザックザク！お祝い！ 🎊<br>
                    <div class="flex justify-center gap-2 mt-2">
                        <div class="flex flex-col items-center bg-indigo-950 px-2 py-1 rounded-lg border border-indigo-700"><span class="inline-flex relative w-5 h-5 mb-1"><img src="草核.png" class="absolute inset-0 w-full h-full object-contain bg-transparent z-10" onerror="fb(this,'✨','text-xs')"></span><span class="text-yellow-400 font-black">×${earnedTickets}</span></div>
                        <div class="flex flex-col items-center bg-indigo-950 px-2 py-1 rounded-lg border border-indigo-700"><span class="inline-flex relative w-5 h-5 mb-1"><img src="草コイン.png" class="absolute inset-0 w-full h-full object-contain bg-transparent z-10" onerror="fb(this,'💰','text-xs')"></span><span class="text-yellow-400 font-black">×${earnedCoins}</span></div>
                        <div class="flex flex-col items-center bg-indigo-950 px-2 py-1 rounded-lg border border-indigo-700"><span class="inline-flex relative w-5 h-5 mb-1"><img src="草の化石.png" class="absolute inset-0 w-full h-full object-contain bg-transparent z-10" onerror="fb(this,'🦴','text-xs')"></span><span class="text-yellow-400 font-black">×${earnedMats}</span></div>
                    </div>`;
            }
        }
        
        document.getElementById('game-end-desc').innerHTML = rewardText; modal.classList.remove('hidden'); modal.classList.add('flex');
        shootConfetti('end-confetti-container', 80); setTimeout(() => { positionTutorialArrow('btn-tutorial-exit'); }, 300); updateHeaderUI(); saveGame(); 
    }
}

window.onload = () => { loadGame(); navTo('screen-home'); setTimeout(checkAndStartTutorial, 600); setTimeout(checkLoginBonus, 1200); initFirebase(); };
</script>
</body>
</html>
